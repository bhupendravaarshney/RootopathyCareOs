# Git position

This repository is delivered with a Git bundle containing the exact initial checkpoint.

- Branch: `main`
- Commit: run `git rev-parse HEAD` after opening the repository; the delivery report records the same hash
- Release status: engineering foundation only; no production freeze tag
- Working tree expected state: clean, except locally generated or ignored dependencies/build output
