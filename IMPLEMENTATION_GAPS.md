# CareOS implementation gap ledger

**Audit date:** 13 September 2026; implementation evidence updated 18 September 2026
**Overall status:** **Phase 0 repository mechanics complete; exact Module 1 input package approved; checksum-bound authorization release, M1-20 membership read/change/owner-transfer, approved mandatory-role MFA enforcement, approved responsive M1-20 table/card adaptation, approved shared M1-01 through M1-04 identity/access conformance, exact M1C readiness projection, and exact M1-07 organization-profile vertical slice complete; additive facility-scope contract ready but not approved; M1B, M1C, and all product modules remain implementation-incomplete; target-environment production acceptance remains open**

This ledger reconciles the repository with the authoritative 46-page _CareOS Complete Build Specification_, the repository architecture/roadmap documents, and the checked-in implementation. It is intentionally evidence-based: a clickable route, a database table, or a dependency is not counted as a completed production capability unless the behavior is persisted, authorized, tenant-safe, audited, and covered by the required tests.

## Audit scope and interpretation

Reviewed:

- all maintained backend, frontend, migration, Compose, CI, script, test, and Markdown files;
- the full 46-page build specification PDF;
- the generated frontend lockfile through a clean `npm ci` installation;
- currently available quality gates.

The current source correctly describes itself as a **runnable engineering foundation**. Its Phase 0 mechanics are complete, and `m1-candidate-1` has an independently verified approval record. Flyway V20 promotes the approved interactive authorization policy and implemented identity/organization-core bindings; V21 adds the exact approved M1-20 membership-read operation; V22 adds governed organization-wide non-owner membership role changes and revocation; V23 adds final-owner-safe owner promotion/demotion; V24 enforces the approved mandatory-role MFA and self-disable rules; V25 completes the approved M1-07 profile fields and persistence boundary. M1-20 implements the approved wide-table/drawer-card result alternative without changing authority, and the shared M1-01 through M1-04 frames implement the approved font/content, hash-safe navigation, route-heading focus, focused local-validation summary, and exact invalid-field association contracts. Phase 1AK starts M1C with the exact ordered live-readiness catalogue; Phase 1AL adds exact legal/display/trading identity, organization type, ISO country, IANA timezone, BCP 47 locale, lifecycle projection, caller editability, stable field errors, sorted evidence, and the real profile-complete evaluator. Every other evaluator without authoritative persistence remains explicitly blocked or not applicable. The remaining facility-scope ambiguity is captured as an approval-ready additive candidate at digest `76a3f7a2c63cef0cab02b8a1d38a66220eee0abb99c9be0a82b64eaa8941fddb`, but its verifier correctly denies implementation authority. The route inventory is still not a completed Module 1, Module 2, or clinical workflow.

| Area                     | Current evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Completion assessment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Backend API              | All 31 implemented operations are checked: two public prototype operations; session, recovery, MFA self-service, and organization-selection identity operations; approved-registry invitation issue/revoke/acceptance; three organization-scoped administrative MFA-reset transitions; three bounded organization readiness/profile operations; the authorized M1-20 membership list/read operation; three governed non-owner membership-change transitions; and three governed owner-transfer transitions. The existing login/session/enrollment operations now expose and enforce mandatory enrollment without adding an unapproved endpoint.                                                                                                                                                                                                                                                                                                                                                                                                                     | Foundation plus five approved M1B identity/access increments and bounded M1C starting mechanics; most production business API families remain unimplemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Backend business modules | Layered prototype, identity, tenancy, governance, platform-capability, and bounded administration packages are present and ArchUnit-enforced; V20 binds implemented identity/organization-core behavior to approved policy, V21 adds a bounded membership-read projection, V22 adds governed organization-wide non-owner role-change/revocation orchestration, V23 adds final-owner-safe owner promotion/demotion, V24 adds live mandatory-role MFA enforcement, and V25 completes the exact approved organization-profile aggregate boundary, but no complete production business module exists | Foundation plus partial M1B and two bounded M1C organization-core slices                                                                 |
| Database                 | Twenty-five migrations and 39 tables, PostgreSQL 18 UUIDv7 defaults for all 17 database-generated identifier columns, separate migration/runtime roles, forced RLS on all 19 current tenant-owned tables, environment-scoped synthetic fixtures, immutable approval/evidence/governance controls, runtime-read-only catalogs, final-owner protection, approved invitation/MFA assurance and approval invariants, an execute-only mandatory-role MFA requirement projection, a database guard against ungoverned factor removal, disjoint reference service identities, exact organization-profile fields/constraints/revision enforcement, approved membership read, database-bound governed non-owner membership changes, and immutable owner-transfer requests | Phase 0 mechanics and approved authorization/profile release verified; most Module 1 business schemas and production provider controls remain incomplete |
| Authentication           | Persisted credentials, Redis sessions, CSRF/origin enforcement, reset, encrypted TOTP/recovery codes, recent authentication, throttling, revocation, approved-registry governed invitations/account linking, service-identity reference authorization, maker-checker administrative MFA reset, tenant-authorized membership read, governed membership changes/owner transfer, and live role-level MFA enforcement are implemented. Mandatory-role accounts without a factor receive a restricted enrollment session, cannot reach organization data, and are upgraded only after verified enrollment; promoted accounts lose stale password-only access.                                                                                                                                                                                                                                                                                                                                                                                                            | Approved mandatory-role MFA and self-disable rules are enforced. Facility-scoped grants, optional viewer/editor organization-policy semantics, production service provisioning/worker wiring, visual acceptance, and target-environment activation remain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Frontend                 | The fail-closed session feature drives identity/session behavior, including a locked `mfa_enrollment_required` state and one-time recovery-code acknowledgement before workspace bootstrap. M1-02 offers approved invitation roles, the root rejects unregistered protected hashes without loading a business page, and the shared identity/workspace frames use the approved font stack, hash-safe keyboard navigation, route-heading focus, and problem-summary focus precedence. Local identity failures focus a durable summary and associate the exact invalid field. M1-05 renders exact-catalogue readiness metrics/priority exceptions, M1-06 renders all 15 ordered gates and fails closed on drift, M1-07 renders the exact approved editable or permission-projected read-only profile, and M1-20 renders its action-heavy table at 768px+ and equivalent cards at 390/320 from the same server projection/action component. The exact 1440/1024/768/390/320 matrix checks every route for overflow/Axe findings. | Identity/session mechanics, shared conformance, mandatory enrollment, exact readiness/profile M1C slices, and the bounded M1-20 read/change/owner-transfer responsive experience pass; remaining readiness evaluators, persisted validation/activation, facility-scoped grants, separately authorized invitation operations, owner acceptance, and all later business screens remain. |
| M1 Administration        | 23 routes and screen metadata; the exact eight-artifact input package is approved; V20 promotes the interactive registry; M1-01 through M1-04 use persisted identity/selection APIs and their shared approved frame/content/focus/validation contract; M1-02/M1-03 high-risk administration binds to approved operations and mandatory-role MFA enforcement; M1-05/M1-06 use the exact ordered live-readiness projection; M1-07 now implements exact approved profile fields, validation, persistence, editability, evidence, and its profile-complete evaluator; M1-20 has authorized membership read, governed organization-wide non-owner role-change/revocation, final-owner-safe owner-transfer UI, and the approved table/card responsive projection. | Input blocker cleared, five M1B runtime increments plus three frontend-conformance increments and two bounded M1C increments complete; M1B facility scope, separately authorized invitation inspection/resend/delivery behavior, and owner/target acceptance remain, while the rest of M1C-M1G still requires missing persistence, evaluators, APIs, final UI states, attacks, regression, and owner acceptance. |
| M2 Workforce             | 29 routes and screen metadata                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | No persisted M2 behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| M3-M13                   | No screens, module packages, schemas, APIs, or tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| COS clinical assessment  | 27 synthetic placeholder routes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Not production assessment workflow                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Runtime operations       | ECS JSON console logs, safe route-template request completion events, bounded W3C trace context, authenticated Prometheus metrics, public status-only liveness/readiness groups, a Redis outage drill, and a repository runbook are implemented. External telemetry, alert routing, backup/restore, and production ownership are not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Repository-local signal baseline verified; production operational acceptance incomplete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Tests                    | 181 backend tests, 57 frontend unit tests, 95 browser/Axe tests across all five required widths, 4 frontend-boundary tests, 9 API-contract tests, 14 Module 1 input-gate tests, 7 Module 1 review-draft tests, 8 Module 1 candidate-package tests, 7 facility-scope candidate tests, and 13 CI-security contract tests | Phase 0 plus approved-registry identity/access, mandatory-role MFA, membership read/change/owner-transfer, and bounded organization-core mechanics pass, including exact readiness/profile validation, legacy blocking, permission editability, field Problems, database constraints, governed update/replay/conflict/evidence, focused identity validation, enrollment, promotion invalidation, factor-disable attacks, signed cursors, actor separation, approval consumption, final-owner safety, containment, and evidence attacks; the facility contract has no runtime authority, and production workflow/environment acceptance remains outstanding |

## What is already usable

- Java/Spring Boot, React/TypeScript, PostgreSQL, Redis, MinIO, Mailpit, Docker Compose, Flyway, and GitHub Actions scaffolding exist.
- The frontend has a responsive visual shell, authenticated session gate, mandatory-role enrollment lock, server-derived expiry lock and event-driven resume revalidation without polling, public password recovery, recent-authenticated MFA enrollment/recovery-code self-service, a server-backed organization switcher, focused field-associated identity validation with hash-safe recovery links, accessible error/loading/denied states, screen navigation/search, keyboard focus styling, all 79 registered route states, a real M1-20 membership list with server-backed filtering/cursor paging and governed non-owner role-change/revocation plus owner-promotion/demotion request/approve/execute states, an approved wide-table/drawer-card result projection with identical server-authorized actions, a focused fail-closed state for unregistered protected hashes, an explicit no-real-data synthetic boundary for remaining generic pages, and exact reference-shell coverage at 1440, 1024, 768, 390, and 320 pixels.
- The backend independently exposes the 79-screen registry at `/api/public/prototype-screens`.
- A 39-table database foundation exists for browser and service identity, organizations/memberships, authorization and operation/event policy, checksum-bound registry releases, invitation/token lifecycle, governed non-owner membership-change and owner-transfer requests, mandatory-role MFA projection/enforcement, maker-checker approval, governance delivery/deduplication, encrypted notification state, and append-only document evidence.
- New application-generated identifiers use a shared RFC 9562 UUIDv7 generator with process-local monotonic ordering; Flyway V13 establishes 12 native defaults, V16/V17/V22 add four more, and V23 adds the owner-transfer request default without rewriting or rejecting historical identifiers.
- All 31 current operations have a checked OpenAPI 3.1 version 0.15.0 contract, validated correlation IDs, and RFC 9457 validation/security error responses. Shared tenant-route, cursor/filter, concurrency, idempotency, retry, session-lifecycle, exact readiness-catalogue, and exact organization-profile conventions plus checked generated frontend types/client are in place for future operations.
- Browser identity uses persisted credentials and Redis sessions with CSRF/origin enforcement, throttling, password recovery, encrypted TOTP, one-use recovery codes, recent authentication, revocation, governed invitations/account linkage, database-backed maker-checker administrative MFA reset, and approved mandatory-role MFA enforcement. Reset, invitation, enrollment, and recovery material remain one-use or memory-only as appropriate.
- The separate service-identity boundary authorizes an exact credential digest, tenant, purpose, non-interactive role, and operation without accepting a human session or membership; provisioning, rotation, and worker activation remain production-owned work.
- Disposable PostgreSQL 18 tests verify Flyway migrations, restricted runtime credentials, actor-only membership discovery, transaction-bound membership/permission authorization, cross-tenant RLS denial, atomic governance evidence, concurrent request replay and consumer delivery, outbox delivery transitions, encrypted notification lifecycle transitions, immutable tenant-bound quarantine/scan/promotion/access-grant/retention evidence, and append-only consumer receipts.
- Nine document/notification/job/worker/scheduler capabilities have typed ports, validated metadata, exact status registration, and default adapters proven to fail before I/O. Private quarantine, malware scanning, clean-document promotion, signed document access, immutable document retention/legal-hold application, durable Redis job transport, and encrypted PostgreSQL notification persistence/leasing have disabled-by-default opt-in adapters with isolated integration coverage; worker execution and scheduler execution retain unavailable adapters. The mandatory PostgreSQL evidence store persists quarantine metadata, scanner attestations, clean-promotion proof, URL-free signed-access grant evidence, and provider-version-hashed retention proof without exposing a public workflow.
- Frontend API generation drift, strict indexed-access type checking, linting, formatting, unit tests, production build, dependency audit, and installation currently pass. See [verification snapshot](#verification-snapshot) for limits.
- CI dependencies are full-SHA pinned and least-privilege bounded; dependency review, CodeQL, Trivy source/image checks, CycloneDX image SBOMs, Dependabot coverage, digest-pinned containers, and non-root runtime contracts are checked into the repository. Hosted enforcement still requires a real target-repository run.
- The backend emits ECS JSON with correlation/trace context, exposes public status-only `/livez` and PostgreSQL/Redis-aware `/readyz`, and provides authenticated Prometheus metrics. OTLP export is disabled by default; `docs/OPERATIONS.md` records the signal contract and production acceptance checklist.
- Spring Security and Nginx now enforce explicit response-header policies, the frontend CSP is strict and same-origin, the Nginx API proxy is bounded, and a production-only startup guard rejects local profiles/material and insecure database, Redis, SMTP, cookie, browser-origin, S3, or enabled-OTLP settings. `docs/PRODUCTION_SECURITY.md` records the deployment acceptance boundary.

These are valuable starting points. None are a substitute for the production behavior below.

## Priority 0 - complete target-environment foundation acceptance

Module 1 implementation is authorized, but no production deployment should be accepted until the remaining environment-owned items below are complete and verified.

| Gap                                                     | Evidence in the repository                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Required completion outcome                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Real identity, sessions, invitations, MFA, and recovery | The persisted browser lifecycle is attack-tested through recovery, MFA, deadlines, membership selection, and tenant-authorized membership read/change. V15 implements governed invitation issue/revoke/one-use acceptance and linkage; V17 implements maker-checker reset; V20 binds both to approved roles/events and separate recent-MFA assurance; V21 binds M1-20 read; V22 binds governed organization-wide non-owner role changes and revocation; V23 binds final-owner-safe owner promotion/demotion; V24 enforces mandatory MFA for the six approved roles, invalidates stale password-only sessions after promotion, and blocks ungoverned enabled-factor removal. React drives browser transitions through the checked client. | Complete M1-20 facility-scoped grants and final UI acceptance; establish production invitation/service-credential delivery/rotation and support procedures; retain `Secure` cookies outside local HTTP development. Define a separate exact contract only if optional viewer/editor organization enforcement or self-disable is desired.                                                               |
| Enforced tenant context and authorization               | Actor-bound discovery and `TenantAuthorizationOperations` remain transaction-bound and deny unknown policy. V20 activates the checksum-approved interactive registry with operation risks, denial modes, reasons, recent authentication/MFA, delegation ceilings, database final-owner safeguards, and immutable release evidence. V21 activates `access.membership.read`; V22 adds exact request/approve/execute bindings for non-owner role change/revocation; V23 adds owner-transfer bindings, approval consumption, executor binding, and final-owner-safe transition enforcement. Production still rejects reference activation.                                                                                                   | Bind each future business handler/event to the approved catalogue, approve the exact checked facility-grant contract before implementing facility scope, and extend operation-specific allow/deny/MFA/maker-checker attacks.                                                                                                                                                                           |
| Effective PostgreSQL RLS                                | Forced RLS covers all 19 current tenant-owned tables, including V22 `membership_change_requests` and V23 `owner_transfer_requests`. PostgreSQL 18 tests prove missing-context and cross-tenant denial plus invitation, service-identity, authorization-approval, membership-change/owner-transfer, organization-profile, governance, notification, consumer, and document-evidence attacks. Composite tenant links and runtime triggers restrict mutations to exact authorized operation/context.                                                                                                                                                                                                                                        | Retain these controls and composite tenant foreign keys for every new tenant table, then prove isolation through authenticated HTTP attack tests for each protected business API.                                                                                                                                                                                                                      |
| Real persistence and transaction boundaries             | Verified tenant transactions protect governance, notification, document-evidence, bounded organization-profile, membership-change, and owner-transfer persistence. Membership changes use `lock_version`/strong ETags and database-enforced workflow state machines. There are still no complete production business-module entity families, repositories, services, or lifecycle state machines outside the delivered bounded slices.                                                                                                                                                                                                                                                                                                   | Implement vertical business slices with PostgreSQL persistence, server validation, `lock_version`/ETag conflict behavior, and transactional use cases.                                                                                                                                                                                                                                                 |
| Audit, outbox, and idempotency implementation           | V6/V9 provide reusable registries, atomic mutation/outbox/idempotency, delivery, and consumer deduplication. V20 activates approved mappings for invitation/MFA/profile mutations; V22 adds exact changed/revoked evidence; V23 adds exact final `identity.owner.transferred` evidence. Implemented mutations replay only exact actor/tenant/operation requests and commit append-only audit/outbox evidence atomically. No destination, scheduler, worker, or consumer transport is active.                                                                                                                                                                                                                                             | Bind every future approved mutation to the executor; approve/activate exact consumers when implemented; provision non-interactive worker dispatch; add destination/consumer adapters, monitoring, governed replay, and retention procedures.                                                                                                                                                           |
| API contract baseline                                   | The checked OpenAPI 3.1 version 0.15.0 document covers all 31 implemented operations, including the exact 15-key readiness contract, exact approved profile response/update/lifecycle fields, mandatory enrollment session state, protected invitation, MFA administration, membership read, non-owner membership change, and owner-transfer request/approve/execute, while retaining tenant paths, cursor/filter, strong ETag/If-Match, scoped idempotency, bounded retry, session lifecycle, and reusable Problem conventions. Generated TypeScript and the CSRF-aware no-auto-retry client are drift-checked. | Apply the checked conventions to each approved protected business vertical slice and add operation-specific schemas, ordering, permissions, concurrency, idempotency, and attack tests. |
| Architecture enforcement                                | Prototype and tenancy code follow domain/application/infrastructure/API packages. Eleven ArchUnit tests enforce backend direction, capability exclusions, and use of the shared UUIDv7 strategy, including rules preventing API code from bypassing durable document-security/promotion/signed-access/retention orchestration or the transactional consumer-inbox boundary. The frontend now has a `features/session` boundary, and a TypeScript-AST CI verifier plus four negative tests enforce inward API/data/component/feature/page imports and reject shared-to-feature, cross-feature, and source-escape dependencies. Production module slices do not yet exist.                                                                 | Extend both enforced patterns to each production backend module and frontend business feature; compose cross-feature behavior only at page/root boundaries.                                                                                                                                                                                                                                            |
| Production document/security/job ports                  | Phase 0F defines all nine fail-closed boundaries. Phases 0G/0H add opt-in S3 quarantine and ClamD scanning; Phase 0I adds opt-in tenant-isolated Redis queue mechanics; Phase 0J adds opt-in AES-256-GCM PostgreSQL notification persistence/leasing; Phase 0R adds mandatory quarantine/scan evidence; Phase 0T adds evidence-gated clean promotion; Phase 0U adds promotion-evidence-gated signed reads plus append-only URL-free grant proof; Phase 0V adds immutable S3 Object Lock COMPLIANCE retention/legal-hold application plus append-only provider-version-hashed evidence. Outbound notification, worker, and scheduler behavior remain unavailable.                                                                         | Approve and harden production storage/scanner/Redis/key-management designs; bind document promotion, access, and retention to an approved business permission/event/state workflow; implement governed retention release/disposal, notification consent/destination/provider delivery, and authorized worker/scheduler adapters; keep each external capability unavailable until its checklist passes. |
| Test isolation and security quality gates               | The backend suite contains 181 tests against disposable PostgreSQL 18, Redis 8, pinned object storage, and deterministic ClamD fixtures; exact readiness/profile, permission, database, replay, conflict, and evidence coverage joins the V24/V25 approved-policy, mandatory-role MFA, membership, invitation, final-owner, tenant, and production-preflight runs. Frontend gates pass 57 unit and 95 exact-width browser/Axe cases, including fail-closed catalogue/profile validation, distinct M1-05/M1-06 projections, exact M1-07 update/read-only states, focused identity validation, shared focus/skip behavior, and M1-20 table/card alternatives. Target-environment acceptance suites remain absent. | Retain disposable isolation and add unit, integration, contract, security, browser, and workflow attacks alongside every business slice and production adapter activation. |
| Reproducible build and CI security                      | Maven Wrapper 3.3.4 pins Maven 3.9.11; exact Node 24.15 and Java 25 gates pass; full-SHA workflows run contracts, browser, dependency review, CodeQL and Trivy; both digest-pinned images build non-root, scan clean at the configured fixed HIGH/CRITICAL threshold, produce CycloneDX SBOMs, and pass isolated hardened-runtime smokes. The image build skips tests because tests are a separate required job.                                                                                                                                                                                                                                                                                                                         | Keep all gates green; run and require both workflows in the target repository; approve SBOM retention, signed provenance, registry admission and deployment verification; add relevant performance checks before Phase 0 acceptance.                                                                                                                                                                   |
| Production HTTP and configuration boundary              | Phase 0O adds explicit Spring API headers, an always-on strict same-origin Nginx CSP/header set, bounded proxy behavior, required production dependency/identity configuration, and a startup guard for HTTPS origins, secure cookies, PostgreSQL/Redis/SMTP/conditional-OTLP transport, non-production secret material, unsafe S3 overrides, and incomplete promotion/signed-access/retention activation.                                                                                                                                                                                                                                                                                                                               | Select and verify the real secret manager, TLS ingress and trusted proxy path, private backend/management network, service-to-service encryption, operation-specific and edge rate limits, WAF/DDoS policy, certificate renewal, CSP reporting, and environment-specific security/load tests.                                                                                                          |
| Operational observability and recovery                  | Phase 0N adds ECS JSON logs, safe request metadata, bounded OpenTelemetry context with all OTLP exporters disabled, authenticated Prometheus format, dependency-correct probes, an outage integration test, hardened image smoke, and a repository runbook. No monitoring/trace backend, scraper identity, alert route, backup, or timed restore exists.                                                                                                                                                                                                                                                                                                                                                                                 | Approve service objectives, telemetry access/region/retention and non-interactive identity; deploy and test collection, dashboards, alerts/on-call escalation, encrypted backups, timed restores, rollback/forward-repair, and provider-specific incident runbooks.                                                                                                                                    |

## Phase 0A progress - frontend quality gate resolved

The first implementation slice was completed on 13 September 2026. `npm run test:e2e` now passes all **8 tests** across desktop and 320px projects while traversing all 79 prototype routes.

| Initial finding                                     | Implemented resolution                                                                                       | Current state                                                   |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------- |
| Serious WCAG contrast failure on active sidebar IDs | Active IDs now use the higher-contrast `--mint-soft` token.                                                  | PASS in Axe route sweeps                                        |
| Mobile test did not open the responsive drawer      | The 320px project now declares mobile/touch semantics and uses an exact workspace-link selector.             | PASS                                                            |
| One 79-route scan exceeded the per-test timeout     | Accessibility checks are split into bounded M1, M2, and COS test cases with route-specific failure messages. | PASS                                                            |
| Browser/Axe checks were not a CI gate               | CI now installs Chromium, runs Playwright/Axe, and uploads HTML reports/traces on failure.                   | Configured and locally verified                                 |
| Frontend toolchain was patch-ambiguous              | Node `24.15.0` is declared in `.nvmrc`, package engines, Docker, and CI; Docker uses `npm ci`.               | Repository aligned; local audit host still needs a Node upgrade |

This closes only the prototype frontend verification slice. It does not satisfy Phase 0 security, tenancy, persistence, or cross-tenant exit criteria.

## Phase 0B progress - architecture, API, and tenant/RLS foundation resolved

The second implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                               | Implemented resolution                                                                                                                                                                                                 | Current state                                          |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| Backend boundaries existed only as an architectural statement | Prototype and tenancy code now use domain/application/infrastructure/API layers; five ArchUnit rules enforce inward dependencies and cycle freedom.                                                                    | PASS                                                   |
| No stable HTTP error or tracing contract                      | A highest-precedence filter validates/creates `X-Correlation-Id`; controller advice and the security filter chain emit safe RFC 9457 problem details.                                                                  | PASS for all implemented public and identity endpoints |
| No machine-checked API description                            | `contracts/openapi/careos-foundation.json` describes both current public operations and shared response schemas; a dependency-free CI verifier checks operations, references, correlation headers, and problem fields. | PASS for the foundation API                            |
| Runtime used the database bootstrap superuser                 | Compose and Spring configuration now use `careos_migrator` for Flyway and restricted `careos_app` credentials for the runtime pool.                                                                                    | PASS in a fresh deployment smoke test                  |
| RLS settings were never applied by application code           | Actor discovery and `TenantAuthorizationOperations` use fresh transactions and PostgreSQL `set_config(..., true)` for organization, actor, purpose, and correlation settings.                                          | PASS                                                   |
| No migration/RLS attack tests                                 | A disposable PostgreSQL 18 suite applies all real migrations and runs twelve role/isolation/authorization attack scenarios.                                                                                            | PASS                                                   |
| Flyway was present but did not run under Spring Boot 4        | The supported Boot Flyway starter now triggers and orders migrations before JPA/custom database adapter startup.                                                                                                       | PASS; schema now reaches version 17                    |

Phase 0B closed the database-role and foundational RLS mechanics. Later slices add membership authorization, governance/idempotency, fail-closed adapters, document evidence/promotion/access/retention, consumer deduplication, and UUIDv7. Phase 0X established reference authorization, governed invitations, disjoint service identities, final-owner controls, and maker-checker MFA administration; Phase 1AB promotes the approved interactive registry and implemented bindings; Phase 1AC delivers bounded M1-20 membership read; Phase 1AD delivers governed non-owner membership changes; Phase 1AE delivers final-owner-safe owner transfer; Phase 1AF captures non-authorizing facility-scope semantics; and Phase 1AG enforces approved mandatory-role MFA. Facility-scope approval/runtime behavior, template/consumer/retention activation, most protected business routes, governed hold release/disposal, real providers/workers/integrations, production infrastructure acceptance, and later business-slice attack tests remain absent.

## Phase 0C progress - browser identity and session core resolved

The third implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                      | Implemented resolution                                                                                                                                                                                                                                                               | Current state                                                                                                                                                                      |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| In-memory HTTP Basic bootstrap                       | Persisted password credentials and a local-profile-only synthetic bootstrap account now feed a JSON login endpoint; Basic and default form login are disabled.                                                                                                                       | PASS                                                                                                                                                                               |
| No production browser session controls               | Spring Session stores indexed sessions in Redis with `CAREOS_SESSION`, HttpOnly, `SameSite=Strict`, rotation, idle/absolute expiry, logout evidence, and security-version revocation.                                                                                                | PASS against disposable Redis 8                                                                                                                                                    |
| No mutation request defense in depth                 | Every unsafe identity request requires a configured Origin/Referer and matching XSRF cookie/header; security failures use RFC 9457.                                                                                                                                                  | PASS in HTTP attack tests                                                                                                                                                          |
| No recovery or MFA lifecycle                         | One-use peppered password-reset tokens, encrypted TOTP seeds, one-use hashed recovery codes, recent authentication, and login/MFA rate limits are implemented. Phase 0P exposes recovery and enrollment/recovery-code self-service; Phase 0X adds maker-checker administrator reset. | PASS for the reference checkpoint; Phase 1AG later enforces the approved mandatory-role MFA/self-disable rules. Optional viewer/editor organization enforcement remains undefined. |
| Authentication evidence was absent                   | Authentication metadata is hashed and written to a database-enforced append-only table; logout updates session evidence.                                                                                                                                                             | PASS, including owner-level update/delete attacks                                                                                                                                  |
| Identity endpoints were outside the checked contract | The OpenAPI 3.1 contract and dependency-free verifier now cover all 21 public, identity, selection, invitation, and administrative-MFA operations and their cookie/CSRF/error contracts.                                                                                             | PASS                                                                                                                                                                               |
| Invitations had no persistence boundary              | V15 supplies permission-governed issue/revoke/acceptance and existing-account linkage with RLS, expiry, one-use token indexing, idempotency, notification, and audit/outbox evidence; V20 rebinds it to approved roles/events plus separate recent MFA.                              | Approved-policy mechanics PASS; production delivery/target-environment acceptance remains                                                                                          |

This closed the browser credential/session/MFA core at that checkpoint. Phase 0D later closed membership-backed selection/authorization mechanics, Phase 0X closed reference invitation/linkage, service-identity authorization, and administrative-MFA mechanics, and Phase 1AA/1AB later approved and promoted the interactive policy. Target-environment production activation remains open.

## Phase 0D progress - membership selection and authorization mechanics resolved

The fourth implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                               | Implemented resolution                                                                                                                                                                                                                          | Current state                                                        |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| Organization selection trusted no real membership source      | Actor-bound, read-only transactions plus forced RLS expose only current active/effective memberships and draft/active organizations.                                                                                                            | PASS                                                                 |
| Tenant context could be entered through a pre-authorized hook | `TenantAuthorizationOperations` locks/revalidates live membership, verifies role and permission status/grant, and runs work in the same tenant transaction.                                                                                     | PASS                                                                 |
| No selection API or header-confusion defense                  | Authenticated list/select APIs use session state only for navigation; forged `X-Organization-Id` values are ignored and suspended memberships disappear immediately.                                                                            | PASS in HTTP attack tests                                            |
| Runtime policy could have become application data             | Three migration-owned catalogs are runtime-read-only and empty by default; unknown roles and permissions deny access.                                                                                                                           | PASS, including runtime INSERT rejection                             |
| Owner policy was missing                                      | V14 provided an executable least-privilege proposal with `reference` status, explicit opt-in, delegation/final-owner controls, and production rejection; V20 later replaces its interactive production role with the checksum-approved release. | Reference mechanics retained; approval gap resolved by Phase 1AA/1AB |

This closed organization selection and generic enforcement at that checkpoint. V14-V17 later added reference permission, role, delegation, final-owner, event, and maker-checker mechanics; Phase 1AA/1AB subsequently approved and promoted their interactive replacement.

## Phase 0E progress - governance evidence and delivery mechanics resolved

The fifth implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                    | Implemented resolution                                                                                                                                                                                                                                                                  | Current state                                                                                            |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| General governance tables were unused              | `GovernedMutationExecutor` composes authorization, idempotency, first business execution, audit, outbox, and response completion in one transaction.                                                                                                                                    | PASS for the reusable transaction boundary                                                               |
| Audit/outbox names and payloads were unconstrained | Migration-owned event/version catalogs plus PostgreSQL triggers reject unknown/retired events, type mismatches, missing/extra top-level keys, oversize payloads, and transaction-context mismatches.                                                                                    | PASS mechanically; production catalog content remains owner-blocked                                      |
| General audit/outbox evidence could be rewritten   | Audit updates/deletes fail even for the table owner; outbox content is immutable/non-deletable and only controlled delivery transitions are allowed.                                                                                                                                    | PASS in owner-level attack tests                                                                         |
| Idempotency had no application behavior            | Records are scoped by tenant, actor, operation, and key; concurrent duplicates serialize, completed responses replay, changed hashes conflict, failures roll back, and expired keys can be reused.                                                                                      | PASS in PostgreSQL concurrency/failure tests                                                             |
| No publisher state machine existed                 | Tenant-authorized `SKIP LOCKED` claims, unique claim tokens, leases, acknowledgement, bounded retry/backoff, retry exhaustion, and dead-letter transitions now exist. Phase 0S adds atomic consumer deduplication and Phase 0X adds a disjoint service-identity authorization boundary. | PASS mechanically; credential provisioning, scheduler, transports, and runtime wiring remain unconnected |

This resolves the generic mechanics without inventing governance content or silently enabling a placeholder worker. See `docs/GOVERNANCE_EVIDENCE.md` for the enforced contract, V6 upgrade precondition, activation checklist, and remaining integration boundary.

## Phase 0F progress - fail-closed platform capability boundaries resolved

The sixth implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                       | Implemented resolution                                                                                                                                                                                                                                | Current state                                                                                                                                                    |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MinIO topology could be mistaken for document support | Separate tenant-authorized ports now cover private quarantine, scanner verdicts, clean promotion, signed reads, and retention without exposing storage keys.                                                                                          | PASS for the activation boundary; all five document capabilities have separate disabled-by-default mechanics, while hold release and disposal remain unavailable |
| Notification and job work had no durable boundary     | Versioned notification and reference-only job envelopes require internal recipients/references, bounded metadata, deduplication keys, and explicit ports. Phase 0I adds Redis job state and Phase 0J adds encrypted notification persistence/leasing. | PASS for contracts and storage mechanics; no worker or outbound delivery exists                                                                                  |
| Missing dependencies could invite unsafe fallbacks    | Nine default adapters report `UNAVAILABLE` and throw before reading content or performing I/O; no local disk, scan bypass, memory queue, or web scheduler is substituted.                                                                             | PASS in focused contract tests                                                                                                                                   |
| Adapter readiness was implicit                        | `PlatformCapabilityRegistry` requires exactly one probe per capability and rejects missing/duplicate registrations; Actuator info exposes only stable state/reason codes.                                                                             | PASS through the real Spring application context and image smoke                                                                                                 |
| Worker activation lacked an authorization boundary    | Worker/scheduler ports require `AuthorizedTenantContext`, and V16 adds exact tenant/purpose/operation authorization for disjoint non-interactive identities. No worker or scheduler starts automatically.                                             | Reference authorization PASS; provisioning, rotation, deployment, and owner policy remain blocked                                                                |

This closes the definition and safe-default slice, not the production integration work. `docs/PLATFORM_CAPABILITIES.md` records the nine current states, contract invariants, and adapter activation checklist.

## Phase 0G progress - private document quarantine mechanics resolved

The seventh implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                                      | Implemented resolution                                                                                                                                                  | Current state                                                               |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| Object storage existed only in Compose                               | An opt-in `S3PrivateDocumentStorageAdapter` now implements the quarantine port using the pinned MinIO Java 9.0.1 client and explicit JVM HTTP transport.                | PASS mechanically; disabled by default and no upload API exists             |
| Object keys or filenames could become caller-controlled              | Keys are derived only from the authorized organization plus document/version UUIDs; the port accepts no key or filename.                                                | PASS in tenant-separation/key-shape tests                                   |
| Upload integrity and retry semantics were undefined                  | Configured size ceilings, exact stream length, SHA-256 verification, conditional creation, mismatch cleanup, byte-verified replay, and conflict rejection are enforced. | PASS against a real S3-compatible container                                 |
| Storage presence could be mistaken for privacy                       | Activation requires a dedicated bucket with no bucket policy; anonymous reads are denied and HTTPS is mandatory unless a local override is explicit.                    | PASS for adapter mechanics; production IAM/KMS/account controls remain open |
| Quarantine could be mistaken for clean content                       | Only the quarantine capability becomes available; scanner, promotion, signed access, and retention remain explicit unavailable adapters.                                | PASS in Spring registry and enabled-image smoke tests                       |
| Local object-store image was mutable and inaccessible to fresh pulls | Compose and Testcontainers now use a pinned, pullable multi-architecture Quay digest.                                                                                   | PASS for synthetic local compatibility; not production provider approval    |

This established an untrusted private holding area only. Phase 0R later adds append-only quarantine metadata and scan attestations, Phase 0T adds evidence-gated clean-copy mechanics, and Phase 0U adds internal signed-read mechanics. There is still no business document/provenance state machine, governed promotion/read workflow, retention enforcement, provider-level encryption/IAM acceptance, or clinical/credential upload workflow.

## Phase 0H progress - quarantined malware-scanning mechanics resolved

The eighth implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                          | Implemented resolution                                                                                                                                                                                                                                                                                                  | Current state                                                                                                   |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Scanner required storage-vendor access or a download API | `QuarantinedDocumentContentSourcePort` is an internal, closeable application boundary; the S3 adapter revalidates tenant, quarantine state, metadata, size, digest, and ETag before conditional read. A sixth ArchUnit rule prevents API-package dependency on the stream.                                              | PASS mechanically; no upload/download route or document record exists                                           |
| ClamD readiness and signature quality were undefined     | Activation requires bounded configuration, `PING`, strict `VERSIONCOMMANDS`, advertised `INSTREAM`, engine version 1.5.3 or newer, a configured definition time zone, and current/non-future definitions. Identity/freshness is rechecked for every scan.                                                               | PASS in deterministic tests and against the pinned official daemon; production signature operations remain open |
| A daemon `OK` could be trusted without content integrity | The adapter streams bounded network-order chunks, enforces a total ceiling/timeouts, independently verifies exact bytes and SHA-256, and returns `CLEAN` only for exact `stream: OK` after both checks.                                                                                                                 | PASS for clean, infected, malformed/error, digest drift, oversize, outage, and timeout paths                    |
| Scanner failure could degrade to bypass                  | Stale definitions, old engines, missing protocol support, storage errors, network failure, timeouts, malformed replies, and integrity mismatch all yield `ERROR` or fail activation; none yields `CLEAN`.                                                                                                               | PASS in seven scanner scenarios                                                                                 |
| Local scanner topology could expose unauthenticated TCP  | The optional overlay pins the official multi-architecture ClamAV 1.5.3 base image by digest, persists signatures, and exposes 3310 only inside Compose. The default stack keeps storage and scanning disabled.                                                                                                          | PASS for local compatibility; production segmentation, resource sizing, monitoring, and acceptance remain open  |
| Scan transport could be mistaken for durable evidence    | At this checkpoint promotion, signed access, and retention remained unavailable and nothing persisted the in-memory result. Phase 0R later adds transaction-bound quarantine metadata and scan attestations; Phases 0T/0U add opt-in promotion and signed-read mechanics without adding a governed business transition. | Evidence, promotion, and signed-read mechanics now PASS; workflow remains incomplete                            |

This resolves scanner transport and integrity mechanics only. Phase 0R supplies authoritative quarantine metadata and durable append-only scan attestations, Phase 0T supplies clean-promotion mechanics and policy snapshots, Phase 0U supplies internal signed-read mechanics and URL-free grant proof, and Phase 0V supplies immutable retention/legal-hold-enablement mechanics. Production completion still needs approved producers and promotion/access/retention policy, business workflow orchestration, governed hold release/disposal, provider object-immutability controls, scanner/signature availability operations, governed audit/outbox transitions, monitoring, recovery, and owner acceptance.

## Phase 0I progress - durable Redis job transport mechanics resolved

The ninth implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                                            | Implemented resolution                                                                                                                                                                                                                                                                  | Current state                                                    |
| -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| Queueing could fall back to memory or Redis presence could imply readiness | The opt-in adapter requires bounded configuration, an explicit `jobType@schemaVersion` allow-list, exact `PONG`, and scripting/server-time support. The unavailable default remains when disabled.                                                                                      | PASS for strict activation and Spring capability replacement     |
| Job state transitions and deduplication were undefined                     | Tenant hash-slot keys plus atomic Lua scripts implement scheduled enqueue, exact deduplication, leased claim, idempotent acknowledgement, retry, maximum-attempt dead-lettering, and terminal cleanup. Raw deduplication tokens never appear in key names.                              | PASS sequentially and under concurrent producers/claimers        |
| Lease loss could duplicate or silently lose work                           | Opaque 256-bit lease tokens, Redis server-time expiry, bounded exponential retry, and expired-lease recovery reject stale acknowledgement and eventually dead-letter abandoned maximum-attempt work.                                                                                    | PASS for retry, lease expiry/recovery, and terminal failure      |
| Retained Redis state could drift or cross tenants                          | Each payload has a SHA-256 drift-detection digest and embedded policy snapshot; malformed records are dead-lettered before delivery. Context-derived keys and claim organization checks deny cross-tenant completion. The digest is not a trust boundary against a Redis administrator. | PASS for retained-record drift and tenant separation             |
| Queue failure and depth were operationally opaque                          | Payload-free tenant snapshots and bounded-cardinality Micrometer counters expose mechanics. A paused real Redis dependency times out fail-closed and the same adapter recovers after service restoration; a new adapter instance recovers persisted work.                               | PASS for telemetry, timeout/recovery, and application restart    |
| A web route could become an accidental worker                              | A seventh ArchUnit rule prevents API packages from depending on queue claim/completion types. Worker and scheduler capabilities remain unavailable and no background loop starts.                                                                                                       | PASS; non-interactive identity and worker deployment remain open |

This resolves Redis queue transport mechanics only. Production still requires approved job definitions, authenticated non-interactive workers, operation-specific tenant authorization, governed business/audit/outbox effects, Redis ACL/TLS/HA/persistence/restore acceptance, capacity monitoring, dead-letter inspection/replay procedures, and operational ownership.

## Phase 0J progress - encrypted durable notification persistence mechanics resolved

The tenth implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                                                      | Implemented resolution                                                                                                                                                                                                                                                                                                                                | Current state                                                                       |
| ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| A durable notification contract could still degrade to plaintext or best-effort mail | The opt-in PostgreSQL adapter requires named 256-bit encryption keys, an active key, and an explicit bounded `template@version` allow-list. The default unavailable adapter remains when disabled, and no SMTP/SMS/push provider is invoked.                                                                                                          | PASS for strict activation and separation from outbound delivery                    |
| Sensitive parameters, destinations, and tokens lacked an at-rest contract            | V7 stores only AES-256-GCM parameter ciphertext with a fresh nonce/AAD, internal recipient UUID, SHA-256 content/deduplication/lease hashes, and request metadata. There is no destination or plaintext-parameter column; key-ring configuration supports historical-key reads.                                                                       | PASS for encryption, data-shape inspection, corruption rejection, and key rotation  |
| Notification concurrency and lifecycle were undefined                                | Forced RLS plus database triggers bind enqueue to the authorized transaction, make content/policy immutable, constrain leases/finalization, preserve terminal evidence, and limit deletion to expired retention. Deterministic `SKIP LOCKED` claims, idempotent replay, server-time leases, bounded backoff, exhaustion, and cleanup are implemented. | PASS sequentially, concurrently, and in database attack tests                       |
| A stale/cross-tenant worker could expose decrypted parameters                        | Claims require an active matching writable `AuthorizedTenantContext`, use opaque 256-bit leases with only hashes persisted, decrypt only after tenant/definition/content/AAD checks, and reject stale/cross-tenant completion. Missing historical keys leave rows unclaimed.                                                                          | PASS for RLS, transaction, lease-loss, ciphertext-tamper, and missing-key scenarios |
| Persistence readiness or metrics could expose secrets                                | Startup verifies the table, forced RLS, restricted non-owner/non-`BYPASSRLS` role, required privileges, server time, and zero missing-context visibility. Actuator status is stable/non-secret; metrics and tenant snapshots contain no payload or recipient value.                                                                                   | PASS for safe readiness and bounded telemetry                                       |
| API or web startup could become notification execution                               | An eighth ArchUnit rule denies API dependency on notification claim/completion types. Worker and scheduler adapters remain unavailable, no loop starts, and consent/destination/provider resolution does not exist.                                                                                                                                   | PASS; outbound delivery remains owner/policy blocked                                |

This resolves encrypted persistence and delivery-leasing mechanics only. Production still requires approved template/payload definitions, consent and destination policy, non-interactive identity and tenant reauthorization, provider selection/security/region/idempotency, approved audit/outbox/delivery evidence, key-manager rotation/re-encryption operations, monitoring, backup/restore, dead-letter replay, retention procedures, and operational ownership.

## Phase 0K progress - CI supply-chain and container-runtime hardening resolved

The eleventh implementation slice was completed and locally verified on 13 September 2026.

| Initial finding                                                           | Implemented resolution                                                                                                                                                                                                                                                                                                        | Current state                                                                                |
| ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| Workflow dependencies and runners were mutable                            | Every external Action now uses a full 40-character commit SHA with a release comment; runners use explicit Ubuntu versions; checkout credentials are discarded; default permissions are read-only; and every job has concurrency cancellation and a finite timeout.                                                           | PASS in the repository contract and negative tests                                           |
| Dependency and source security gates were absent                          | Pull requests run dependency review; CodeQL analyzes Java/Kotlin and JavaScript/TypeScript with `security-extended`; Trivy scans Maven/npm dependencies, secrets, and configuration; weekly Dependabot entries cover Maven, npm, Docker/Compose, and Actions.                                                                 | Configured and locally contract-verified; hosted graph/upload execution remains external     |
| Images were built without an SBOM or vulnerability gate                   | CI builds both images, creates CycloneDX SBOMs, uploads them with bounded retention, and rejects fixed HIGH/CRITICAL image findings. Local Trivy 0.74 scans find zero such dependency/image vulnerabilities, zero Dockerfile misconfigurations, and no emitted secret findings.                                               | PASS locally; registry admission, signing/provenance, and hosted artifact policy remain open |
| Mutable image tags remained in build, Compose, mail, and PostgreSQL tests | Every Dockerfile base, Compose/overlay service, and PostgreSQL/Redis Testcontainers image is pinned by SHA-256 digest. PostgreSQL fixtures explicitly declare the official digest as compatible with the Testcontainers module, and all 77 tests pass.                                                                        | PASS for the maintained repository                                                           |
| The managed Tomcat and prior OS layers had fixed high-severity findings   | Tomcat is overridden to the fixed 11.0.25 floor; the backend final stage is Java 25 Debian 13 distroless and the frontend is the patched Nginx 1.30.4 Alpine slim image. No vulnerability exception file was introduced.                                                                                                      | PASS at the configured fixed HIGH/CRITICAL threshold                                         |
| Non-root users were not sufficient runtime confinement                    | The backend runs as UID 65532 and the frontend as UID 101/Nginx on unprivileged port 8080. Both pass read-only-filesystem, all-capabilities-dropped, no-new-privileges smokes; the backend retains an explicit BusyBox health probe without a package manager in the final image.                                             | PASS in isolated final-image smokes                                                          |
| Security configuration could silently regress                             | `scripts/verify-ci-security.mjs` verifies every workflow, Dockerfile, Compose model, dependency-update entry, and pinned PostgreSQL/Redis test image. Eight built-in Node tests prove that mutable references, excess permission, floating runners, missing bounds, root resets, and incomplete update coverage are rejected. | PASS locally and included in the quality workflow                                            |

This resolves the repository-local supply-chain baseline, not hosted or production delivery acceptance. Both workflows must still run successfully in the target GitHub repository with dependency-graph and security-event permissions, become required checks, and feed an approved signed-image/SBOM/registry/deployment policy. Secrets management, production infrastructure, operational recovery, and relevant load/security testing remain separate gaps.

## Phase 0L progress - API convention and generated browser-client baseline resolved

The twelfth implementation slice was completed and locally verified on 14 September 2026.

| Initial finding                                               | Implemented resolution                                                                                                                                                                                                                                                                                                   | Current state                                                                         |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- |
| Protected tenant routes and list behavior were undefined      | OpenAPI `x-careos-conventions` now fixes the organization path prefix, shared UUID/cursor/limit components, opaque cursor behavior, operation filter allowlists, and unknown-filter rejection. `docs/API_CONVENTIONS.md` records stable-ordering and server-bound cursor requirements.                                   | PASS as a checked convention; no protected business list exists yet                   |
| Optimistic concurrency and general retry behavior were absent | Shared strong `ETag`/required `If-Match`, 412/428 Problems, scoped `Idempotency-Key`, bounded 429/503 `Retry-After`, and caller-controlled/no-mutation-auto-retry rules are machine checked. Backend CORS allows/exposes the corresponding headers and removes `X-Organization-Id`.                                      | PASS at the boundary; each real endpoint must implement and attack-test the behavior  |
| Frontend contract types could drift or be hand-maintained     | Exact `@hey-api/openapi-ts` generation produces two TypeScript-only artifacts from the local contract; `api:check` regenerates in isolation and compares file sets/content. The TypeScript 6-compatible generator is pinned, its YAML dependency is overridden to fixed 4.3.2, and exact Node installation audits clean. | PASS locally and included before CI typecheck                                         |
| The React application had no HTTP client                      | A generated-type-backed client wraps all 15 operations, always includes credentials/correlation, bootstraps and validates CSRF before unsafe requests, accepts only documented statuses/media, safely parses Problems, exposes strong ETags/bounded retry delay, and never automatically retries.                        | PASS in eight focused unit tests; Phase 0M now calls the identity/organization subset |
| Contract policy regression had only a happy-path script       | The verifier is importable and six Node tests mutate the checked document to prove ambiguous tenant prefixes, permissive filters, mutation retry, weak preconditions, and broken references fail. CI runs the drift and negative suites.                                                                                 | PASS locally                                                                          |
| Indexed array assumptions remained hidden in the prototype    | `noUncheckedIndexedAccess` is enabled; the screen registry asserts a valid default and page navigation has explicit fallbacks.                                                                                                                                                                                           | PASS in strict typecheck and prototype unit tests                                     |

This closes the reusable HTTP/client integration boundary only. The owner still must approve authorization/event content, and each protected route still needs real persistence, transaction-bound authorization/RLS, operation-specific filtering and state transitions, governance evidence, UI integration, and attack/browser coverage.

## Phase 0M progress - authenticated frontend session boundary resolved

The thirteenth implementation slice was completed and locally verified on 14 September 2026.

| Initial finding                                                                        | Implemented resolution                                                                                                                                                                                                                                                                                                      | Current state                                                                            |
| -------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| The checked client was unused and every hash route exposed a synthetic shell           | A memory-only `features/session` state machine bootstraps the server session before rendering protected content, then loads membership-backed organizations and requires an unambiguous selected context. Anonymous, pending-MFA, no-membership, dependency-failure, and authenticated states are distinct and fail closed. | PASS for the implemented foundation routes; protected business data still does not exist |
| Login, MFA, organization switching, and sign-out controls were synthetic or inert      | M1-01 login, pending M1-03 MFA challenge, M1-04 organization selection, top-bar/mobile organization switching, and logout now call the checked client. Password/code fields have no defaults and are cleared on submit; CSRF/correlation/no-retry behavior remains centralized in the client.                               | PASS in unit and browser transition tests                                                |
| TypeScript response types could be trusted as if they were runtime validation          | The session boundary validates session/user UUID state, organization shape/uniqueness, and at-most-one server selection before exposing the workspace. Malformed or ambiguous responses lock the shell and preserve a support correlation reference. A failed logout does not falsely clear local authenticated state.      | PASS in fail-closed unit tests                                                           |
| Identity errors and organization absence had no accessible UI state                    | Loading, actionable error, retry-after/support reference, explicit selection, and no-membership screens are keyboard-addressable; new error summaries receive focus and the full route/state set is Axe-checked at desktop and 320px.                                                                                       | PASS in the browser suite                                                                |
| The invitation placeholder looked actionable despite having no governed implementation | M1-02 now states that invitation issuance/linkage/expiry/acceptance is unavailable and sends no token or mutation until an approved API exists.                                                                                                                                                                             | Correctly unavailable; invitation completion remains open                                |
| Frontend dependency direction was unenforced                                           | A TypeScript-AST boundary verifier checks all source imports in CI. Four fixture tests prove it rejects shared-component-to-feature reversal, cross-feature imports, API-to-page reversal, and source-boundary escape.                                                                                                      | PASS locally and configured before CI typecheck                                          |

This closes only the browser session/navigation integration boundary. It does not approve final M1 designs, turn organization selection into authorization evidence, implement invitations or MFA/recovery administration, add continuous session revalidation, or provide any protected tenant business API, permission-driven business navigation, server-state record cache, or persisted production workflow. See `docs/FRONTEND_SESSION.md`.

## Phase 0N progress - operational signal foundation resolved

The fourteenth implementation slice was completed and locally verified on 14 September 2026.

| Initial finding                                                             | Implemented resolution                                                                                                                                                                                                                                                                                            | Current state                                                                              |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| Generic health could not distinguish restart from traffic-removal decisions | Public status-only `/livez` uses only application liveness; `/readyz` uses application readiness plus PostgreSQL and Redis. Actuator aliases remain available, details/components are suppressed, and Compose waits on readiness.                                                                                 | PASS in live context, dependency-outage test, and final-image smoke                        |
| Runtime logs were unstructured and had no safe request completion event     | Console output is ECS JSON. Each request records validated correlation plus trace/span context, an allow-listed method, framework route template, status, duration, and outcome; it never records raw URI/query/body/headers, actor, tenant, recipient, or payload values.                                        | PASS in focused metadata/redaction tests and live JSON output                              |
| Metrics had no standard scrape format and tracing had no bounded foundation | Prometheus format is exposed only to fully authenticated sessions with a bounded application tag. OpenTelemetry supplies W3C context at 10% default sampling with baggage disabled and attribute/event/link limits. Trace export and OTLP metric/log export are disabled; inherited `OTEL_*` mapping is disabled. | PASS for local mechanics; production scraper identity and collector acceptance remain open |
| Dependency failure semantics were unproved                                  | A disposable Redis pause makes readiness return `503/DOWN` while liveness stays `200/UP`, then readiness recovers after unpause. PostgreSQL and Redis are required; deliberately optional platform adapters do not make the whole service unready.                                                                | PASS in the full integration suite                                                         |
| No operational guide described signal use or acceptance boundaries          | `docs/OPERATIONS.md` defines probe semantics, safe fields, first response, a local drill, monitoring/alert acceptance, deployment checks, and recovery requirements without inventing thresholds or claiming external services.                                                                                   | PASS for the repository runbook                                                            |

Local evidence: a clean pinned Java 25 build compiled 136 production sources and 11 test sources, passed all 80 tests, and packaged the bootable JAR. Both Compose models plus the 15-operation API, six API negative tests, 79-screen register, CI-security contract, and eight CI-security negative tests pass. The rebuilt distroless image reached `UP` on `/livez` and `/readyz` against fresh PostgreSQL 18 and Redis 8, rejected anonymous metrics, started no OTLP metrics publisher, and ran as UID 65532 with a read-only root filesystem, all capabilities dropped, and no-new-privileges. Trivy 0.74 reports zero fixed HIGH/CRITICAL findings in both the Debian 13.6 layer and packaged JAR. Temporary smoke resources were removed.

This closes the repository-local operational signal mechanics, not production operations. A target environment still needs an approved non-interactive scrape boundary, collector and telemetry policy, service objectives and measured thresholds, dashboards, alert delivery/on-call escalation, managed infrastructure, deployment manifests, encrypted backup ownership, timed restore evidence, dead-letter procedures, and provider-specific incident/rollback runbooks. See `docs/OPERATIONS.md`.

## Phase 0O progress - HTTP and production-configuration hardening resolved

Completed on 14 September 2026:

| Previous gap                                                   | Implemented evidence                                                                                                                                                                                                                                                                                                                                                                                      | Status                                                                                                                                          |
| -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| Backend response policy relied on framework defaults           | Spring Security now declares API CSP, deny-framing, no-referrer, no-sniff, restricted permissions, and secure-request-only one-year HSTS without preloading or subdomain scope.                                                                                                                                                                                                                           | PASS through integration assertions on public API responses                                                                                     |
| The production frontend had no checked CSP/header contract     | Nginx emits ten `always` headers, including a strict same-origin CSP with no wildcard, `unsafe-inline`, or `unsafe-eval`; it also suppresses version tokens.                                                                                                                                                                                                                                              | PASS through repository negative tests and rebuilt-image response inspection                                                                    |
| Proxy failure behavior was unbounded                           | The same-origin `/api` proxy has 3-second connect and 30-second send/read limits, uses HTTP/1.1, and clears the hop-by-hop `Connection` header.                                                                                                                                                                                                                                                           | PASS through checked configuration and Nginx syntax/runtime smoke                                                                               |
| Production could inherit local dependency and browser defaults | Successful synthetic fallbacks and bootstrap values now live only in `application-local.yml`; the base and production configurations require explicit dependency/browser/identity values. A production-only guard rejects mixed profiles, documented local/test material, insecure cookies/origins/transports, embedded database credentials, unsafe S3 overrides, and an insecure enabled OTLP endpoint. | PASS in three focused positive/negative configuration tests plus the base/production repository contract; provider injection remains unaccepted |
| TLS, edge, and abuse ownership was ambiguous                   | `docs/PRODUCTION_SECURITY.md` defines the secret-injection interface, header contract, TLS/proxy responsibilities, and remaining rate-limit/WAF/load/security acceptance.                                                                                                                                                                                                                                 | DOCUMENTED; real deployment controls remain open                                                                                                |

Local evidence: the Java 25 build compiles 137 production sources and 11 test sources, applies Flyway through V7, passes all 84 backend tests, and packages the JAR. Frontend formatting/typecheck/lint/unit/build and all 12 browser/Axe tests remain green. Both Compose models, the 15-operation API plus six negative contract tests, the 79-screen register, and all 10 CI-security contract tests pass. Both rebuilt images have zero fixed HIGH/CRITICAL OS-package findings. The backend image refuses a no-configuration start on the first required placeholder; the unprivileged Nginx image serves the strict CSP and all required headers under read-only/capability/no-new-privileges runtime restrictions.

This closes only policy-neutral repository controls. It does not provide certificates, an ingress, a secret manager, trusted forwarding configuration, service-to-service TLS, a private management plane, application/edge quotas, WAF/DDoS policy, CSP reporting, or provider-specific scans and exercises. Those remain Phase 0 production acceptance work.

## Phase 0P progress - browser identity lifecycle self-service resolved

The sixteenth implementation slice was completed and locally verified on 15 September 2026.

| Initial finding                                                      | Implemented resolution                                                                                                                                                                                                                                                                                                                          | Current state                                                                                                                      |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Password recovery existed only as a backend operation and email link | M1-01 links to a generic reset-request route. The completion route preserves token case, immediately replaces the current history entry with a token-free fragment, enforces matching/size-bounded password input, clears submitted secrets, and reports server-confirmed session revocation.                                                   | PASS in unit, browser, checked-client, and existing backend replay/revocation tests                                                |
| M1-03 supported only the pending login challenge                     | Fully authenticated users can now verify current credentials, enroll and confirm TOTP, view the one-time recovery-code response, and explicitly replace codes after acknowledging that prior codes are revoked. Identity self-service remains available without requiring a tenant selection; Phase 0X later adds governed administrator reset. | PASS for this checkpoint; Phase 1AG later adds restricted mandatory-role enrollment and database-enforced self-disable protection. |
| A pre-enrollment session principal could report stale MFA state      | The checked `SessionState` adds `mfaEnabled`, and every authenticated backend response resolves it from the persisted active account, including an existing session immediately after enrollment.                                                                                                                                               | PASS in backend integration assertions and frontend runtime validation                                                             |
| Generated success types could expose malformed secret material       | The session feature accepts enrollment material only with a bounded `otpauth://totp/` shape and accepts only non-empty, unique recovery codes matching the checked alphabet/group format. Invalid output locks the protected boundary; `401` returns to anonymous and `428` requires recent authentication again.                               | PASS for checked contract/runtime mechanics                                                                                        |
| Browser identity coverage stopped at challenge/selection/logout      | Five new unit scenarios cover generic reset request, token case/scrubbing/consumption, missing/mismatched reset input, enrollment/one-time-code cleanup, and recent-authenticated code replacement. Four desktop/mobile browser cases exercise recovery and enrollment in addition to the existing route/Axe matrix.                            | PASS: 24 unit and 16 browser tests                                                                                                 |

Local evidence: a clean Java 25 build compiles 137 production sources and 11 test sources, applies Flyway through V7, passes all 84 backend tests against disposable dependencies, and packages the JAR. Frontend generated drift, the 18-source/35-import architecture check and four negative fixtures, format, strict typecheck, lint, all 24 unit tests, production build, and all 16 desktop/mobile Playwright/Axe tests pass. The 15-operation OpenAPI 3.1 contract is version 0.5.0 and passes all six convention checks; all six API negative tests, the 79-screen register, and the CI-security contract with all 10 negative tests also pass.

This closed password recovery and MFA enrollment/recovery-code self-service at that checkpoint. Phase 0Q later closed session-expiry convergence, and Phase 0X closes reference invitation/linkage, administrator reset, and service-identity authorization. Self-disable policy, owner-approved production administration, and every tenant business workflow remain open.

## Phase 0Q progress - browser session-expiry convergence resolved

The seventeenth implementation slice was completed and locally verified on 15 September 2026.

| Initial finding                                                                               | Implemented resolution                                                                                                                                                                                                                                                                             | Current state                                                                      |
| --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| Redis idle and application absolute expiry had no browser-readable deadline                   | Valid authenticated responses now expose `X-CareOS-Session-Expires-In`, calculated as the effective seconds to the earlier idle or absolute deadline and exposed through the exact CORS allowlist. Anonymous responses omit it.                                                                    | PASS in focused calculation, CORS, and live identity integration tests             |
| Periodic session polling would keep an unattended idle session alive                          | OpenAPI 3.1 version 0.6.0 adds a checked no-background-polling lifecycle convention. The browser arms a bounded memory-only timer and removes authenticated UI state at the deadline without making a request.                                                                                     | PASS in unit and desktop/mobile browser tests that assert only one session request |
| Later API traffic and revocation could leave a stale client deadline                          | The checked client validates the bounded expiry header on every response, derives a conservative local deadline from request start, publishes deadline changes, and broadcasts every `401` as session invalidation. Authenticated bootstrap fails closed when its deadline is absent or malformed. | PASS in client and application tests                                               |
| A throttled background tab or back/forward-cache restore could resume stale protected content | Focus, visible-tab, online, and persisted-page return events first compare the local deadline. An expired view locks locally; an unexpired view reruns checked session and organization bootstrap before restoring the workspace.                                                                  | PASS for local deadline and resume-revalidation transitions                        |
| Session-expiry behavior was not included in the route-wide browser matrix                     | Five unit scenarios cover header publication, global invalidation, missing-deadline denial, local timeout, and focus revalidation. One new scenario per viewport proves no-polling expiry lock.                                                                                                    | PASS: 29 unit and 18 browser tests                                                 |

Local evidence: a clean Java 25 build compiles 138 production sources and 12 test sources, applies Flyway through V7, passes all 87 backend tests against disposable PostgreSQL 18, Redis 8, object storage, and deterministic ClamD fixtures, and packages the JAR. Frontend generated drift, the 18-source/35-import architecture check and four negative fixtures, formatting, strict typecheck, lint, all 29 unit tests, production build, and all 18 desktop/mobile Playwright/Axe tests pass. The 15-operation OpenAPI 3.1 version 0.6.0 contract passes all seven conventions and all seven negative tests; the 79-screen register and CI-security contract with all 10 negative tests remain green.

This closes automatic browser expiry convergence for the checked client. Future business clients must continue using the shared lifecycle publisher; direct credentialed fetches would bypass the deadline observer and are not permitted. Phase 0X later closes reference invitation/linkage, administrator-reset, and service-identity mechanics; Phase 1AG later closes the approved mandatory-role MFA/self-disable rules. Production acceptance and every complete tenant business workflow remain open.

## Phase 0R progress - durable document-security evidence mechanics resolved

The eighteenth implementation slice was completed and locally verified on 15 September 2026.

| Initial finding                                                                   | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                                         | Current state                                                                                                                                                                       |
| --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Successful quarantine and scanner calls returned only process-memory values       | Flyway V8 adds separate append-only quarantine-evidence and scan-attestation tables. The stored identity is the opaque tenant/document/object-version tuple; quarantine metadata includes exact bytes, media type, digest and request context, while every scan stores verdict, scanner identity, definitions version, observed digest and timestamps.                                                         | PASS for durable mechanics; Phases 0T/0U add promotion and signed-read mechanics, but no business document record, upload/read API, governed document workflow, or retention exists |
| Evidence rows could otherwise be forged outside the authorization boundary        | Both tables use forced RLS, exact tenant policies, restricted `SELECT`/`INSERT`-only runtime grants, actor/purpose/correlation insert triggers, database-owned record timestamps, mutation-rejection triggers, and a composite tenant/document/object-version foreign key. Startup rejects an unsafe runtime role, missing policy/trigger/composite link, excess grants, or visibility without tenant context. | PASS in PostgreSQL 18 role, RLS, cross-tenant, direct-SQL, and migration-owner mutation attacks                                                                                     |
| Storage retry after transaction failure could create ambiguous evidence           | Quarantine writes are exact-replay idempotent. The coordinator verifies the authorized evidence transaction before storage I/O, accepts only the expected opaque reference, records evidence after storage acceptance, rejects changed metadata, and can retry an already verified S3 object after rollback.                                                                                                   | PASS in ordering/conflict unit tests plus database rollback/idempotency tests                                                                                                       |
| Scanner failures and digest drift needed different treatment                      | `CLEAN` and `INFECTED` attestations require the authoritative quarantine digest. `ERROR` is still durably recorded even when content could not be observed and the digest is the explicit unknown value; it can never satisfy later promotion. Duplicate observations converge and contradictory observations at the same scanner/time identity fail.                                                          | PASS for clean, error, missing quarantine, digest mismatch, duplicate, latest-observation, and ambiguity cases                                                                      |
| A future controller could bypass evidence and call storage/scanner ports directly | `DocumentSecurityOperations` is the supported orchestration boundary, and the ninth ArchUnit rule prevents API packages from depending on storage, scanner, or low-level evidence ports.                                                                                                                                                                                                                       | PASS in the architecture suite                                                                                                                                                      |

Local evidence: a clean Java 25 build compiles 146 production sources and 14 test sources, applies all eight migrations to disposable PostgreSQL 18, passes all 99 backend tests against PostgreSQL, Redis, pinned object storage, and deterministic ClamD protocol servers, and packages the bootable JAR. The new slice contributes five application-ordering tests, six PostgreSQL evidence/RLS tests, and one architecture rule. The frontend and checked API surface are unchanged at 29 unit tests, 18 desktop/mobile browser tests, 15 operations, seven conventions, and seven API negative tests.

This closes durable quarantine and scanner evidence mechanics only. All external adapters remain disabled by default, and raw storage or scanner success is still not authorization to expose content. Phase 0T later adds clean-promotion mechanics, Phase 0U adds promotion-evidence-gated signed-read mechanics, and Phase 0V adds immutable retention/legal-hold-enablement mechanics; a production workflow still needs an approved business document model and permission/event/retention registry, governed upload/finalization/promotion/read/hold-release/disposal behavior, provider/IAM/KMS/Object-Lock acceptance, monitoring, and protected HTTP/browser attack coverage.

## Phase 0S progress - durable consumer inbox and deduplication mechanics resolved

The nineteenth implementation slice was completed and locally verified on 15 September 2026.

| Initial finding                                                                            | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                      | Current state                                                                                                                  |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| At-least-once outbox delivery had no matching consumer-side deduplication boundary         | Flyway V9 adds a migration-owned `outbox_consumer_definitions` allow-list and tenant-scoped `consumer_inbox_records` keyed by organization, consumer, and stable source event ID. Production definitions remain intentionally empty.                                                                                                                                                        | PASS for fail-closed mechanics; no transport or consumer is activated                                                          |
| A receipt could be committed separately from its business effect                           | `ConsumerInboxExecutor` enters `TenantAuthorizationOperations`; the PostgreSQL adapter acquires the unique receipt and runs the first consumer callback in that same transaction. Callback failure rolls back both effect and receipt, so a later valid delivery can retry.                                                                                                                 | PASS in rollback/retry integration coverage                                                                                    |
| Concurrent or altered redeliveries could repeat or silently change work                    | PostgreSQL uniqueness serializes concurrent delivery. Exact envelopes, including semantically equivalent canonical JSON, return the original receipt without invoking the callback; changed payload or envelope metadata under the same source ID conflicts.                                                                                                                                | PASS sequentially and with simultaneous virtual-thread deliveries                                                              |
| Consumer payloads could bypass the approved event shape or be copied into receipt evidence | The adapter accepts only an active consumer-to-event-version mapping, validates aggregate type and required/allowed top-level payload keys against the existing event registry, hashes PostgreSQL-canonical JSON with SHA-256, and persists only the digest plus bounded source metadata.                                                                                                   | PASS for unknown consumers, payload drift, canonical replay, and content conflict; nested JSON Schema enforcement remains open |
| Inbox policy or evidence could be weakened at runtime                                      | V9 makes the consumer registry runtime-read-only and receipts append-only under forced RLS with `SELECT`/`INSERT`-only runtime grants, transaction-context/server-time insert enforcement, and owner-level update/delete rejection. Startup verifies the safe role, ownership, exact grants, RLS/policy/triggers, key/FK shapes, database time, and zero visibility without tenant context. | PASS in role/grant/RLS/cross-tenant/direct-SQL/migration-owner attacks                                                         |
| A controller could bypass atomic receipt execution                                         | A tenth ArchUnit rule prevents API packages from depending on the low-level consumer inbox port.                                                                                                                                                                                                                                                                                            | PASS in the architecture suite                                                                                                 |

Local evidence: a clean Java 25 build compiles 153 production sources and 14 test sources, applies all nine migrations to disposable PostgreSQL 18, passes all 106 backend tests against PostgreSQL, Redis, pinned object storage, and deterministic ClamD protocol servers, and packages the bootable JAR. The new slice contributes six PostgreSQL transaction/concurrency/security scenarios and one architecture rule. The 15-operation/seven-convention API verifier and all seven negative tests, the 79-screen register, and the CI-security verifier with all ten negative tests were rerun and pass. The frontend source is unchanged at its latest verified 29 unit tests and 18 desktop/mobile browser tests.

This closes only the reusable inbox and deduplication mechanics. It does not authenticate a worker, discover tenants, subscribe to or verify a transport, populate a production consumer/event registry, authorize a real consumer effect, acknowledge a broker message, expose replay controls, or define retention/monitoring/dead-letter procedures. Those remain required before any publisher or consumer is activated.

## Phase 0T progress - evidence-gated clean document promotion mechanics resolved

The twentieth implementation slice was completed and locally verified on 15 September 2026.

| Initial finding                                                           | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                                                                             | Current state                                                                                                                                      |
| ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| A raw scanner return could otherwise be passed directly to promotion      | `DocumentPromotionOperations` accepts only an opaque document reference, reloads authoritative quarantine and latest scan evidence inside the authorized writable tenant transaction, and requires exact reference/digest equality, `CLEAN`, an accepted scanner, and the configured age/future-skew window.                                                                                                                                       | PASS in seven application ordering/rejection tests; no HTTP route or business permission is added                                                  |
| A private storage copy alone would not prove a valid promotion            | Flyway V10 adds append-only `document_promotion_evidence` with the exact scan-attestation link and a snapshot of policy key, canonical scanner allow-list, maximum age, future skew, actor, purpose, correlation, and PostgreSQL promotion time. Its trigger independently requires the latest scan, `CLEAN`, accepted scanner, and database-time freshness.                                                                                       | PASS for exact replay, changed-policy conflict, older-scan/non-clean/stale/unapproved evidence, and direct-SQL attacks                             |
| Clean content could be confused with quarantine or become public          | The opt-in S3-compatible adapter streams into a distinct clean bucket and tenant-derived `/clean` key, preserves quarantine, requires both buckets to have no bucket policy, conditionally creates the destination, rehashes the source while copying, removes a detected bad copy, and hashes an existing target before accepting an exact retry.                                                                                                 | PASS against pinned MinIO for private access, tenant mismatch, metadata/digest corruption, bad-copy cleanup, exact replay, and conflicting targets |
| Failure ordering could leave a database row claiming an unsuccessful copy | The coordinator copies first and records promotion evidence only after the adapter returns the exact reference. Storage failure leaves no promotion row; a later database/transaction failure can leave only an inaccessible private orphan, which the verified exact-target replay can safely converge.                                                                                                                                           | PASS in coordinator order/reference/evidence-drift tests; operational orphan inspection remains part of production acceptance                      |
| Enabling storage could accidentally enable promotion                      | `careos.documents.promotion.enabled` defaults to false and requires the S3 adapter plus an explicit bounded policy key, scanner allow-list, maximum scan age, and future skew. The adapter rechecks the configured policy and current scan window; production preflight rejects promotion without private S3 storage or required policy inputs. The existing document ArchUnit rule now also prohibits API access to the low-level promotion port. | PASS in Spring capability replacement, invalid configuration, production preflight, and architecture tests                                         |

Local evidence: a clean Java 25 build compiles 161 production sources and 15 test sources, applies all ten migrations to disposable PostgreSQL 18, passes all 119 backend tests against PostgreSQL, Redis, pinned object storage, and deterministic ClamD protocol servers, and packages the bootable JAR. This slice adds seven application tests, three MinIO promotion scenarios, two PostgreSQL promotion/RLS scenarios, and one production-preflight test. Both Compose models resolve. The unchanged 15-operation/seven-convention API verifier and all seven negative tests, the 79-screen register, and the CI-security verifier with all ten negative tests were rerun and pass. Frontend source remains unchanged at its latest verified 29 unit tests and 18 desktop/mobile browser tests.

This closes clean-promotion mechanics only. The adapter and synthetic policy remain disabled by default and are not production approval. Phase 0U later adds internal signed-read mechanics and Phase 0V later adds immutable retention/legal-hold-enablement mechanics, but there is still no M7 document/provenance state machine, approved permission/event/policy entry, governed upload/finalization/read/disposal route, hold-release workflow, object-store IAM/KMS/Object-Lock/backup acceptance, monitoring, or protected HTTP/browser attack coverage. No content is publicly addressable by default.

## Phase 0U progress - promotion-evidence-gated signed document access mechanics resolved

The twenty-first implementation slice was completed and locally verified on 15 September 2026.

| Initial finding                                                            | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                | Current state                                                                                                                                       |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| A raw document reference and TTL could otherwise reach a URL signer        | `DocumentAccessOperations` first requires committed V10 promotion evidence inside the authorized writable tenant transaction, checks tenant and configured purpose policy, and constructs a short-lived bounded in-memory authorization. The low-level port no longer accepts a raw reference.                                                                                        | PASS in six application ordering, denial, signer-failure, response-drift, and evidence-drift tests; no HTTP route or production permission is added |
| A clean-object metadata claim could hide altered content                   | The opt-in S3-compatible adapter requires the expected private clean bucket, validates state/digest/type/size/ETag, downloads the ETag-bound object, and recomputes its full SHA-256 before signing only `GET`. It rejects missing, cross-tenant, wrong-purpose, changed-policy, stale/future authorization, metadata, and content drift.                                             | PASS against pinned MinIO, including successful bounded retrieval, anonymous denial, signed-URL write denial, and post-promotion corruption         |
| Signed URL creation had no durable issuance proof                          | Flyway V11 adds append-only `document_access_grant_evidence`, linked by composite tenant/document/version keys to committed promotion evidence. It snapshots policy, canonical purposes, requested/maximum TTL, authorization-age/skew limits, actor, purpose, correlation, authorization/grant/expiry times, and deliberately stores no URL, signature, credential, or bearer token. | PASS for exact replay, URL-free schema inspection, tenant invisibility, restricted grants, forced RLS, and owner-level mutation attacks             |
| Application clock or a forged row could bypass purpose and lifetime limits | The V11 trigger binds request context, uses PostgreSQL time, independently requires an accepted purpose, recent authorization, exact expiry arithmetic, unexpired grant, plausible promotion time, and an existing promotion row.                                                                                                                                                     | PASS for direct-SQL unapproved-purpose, unpromoted, stale, expiry, conflict, and cross-tenant cases                                                 |
| Storage activation could accidentally enable document delivery             | `careos.documents.signed-access.enabled` defaults to false and requires private S3 storage plus an explicit bounded policy key, purpose allow-list, one-second-to-one-hour TTL, one-second-to-five-minute authorization age, and zero-to-one-minute future skew. Production preflight and the document ArchUnit rule cover the new boundary.                                          | PASS in Spring capability replacement, invalid configuration, production-preflight, and architecture tests                                          |

Local evidence: a clean Java 25 build compiles 170 production sources and 16 test sources, applies all eleven migrations to disposable PostgreSQL 18, passes all 131 backend tests against PostgreSQL, Redis, pinned object storage, and deterministic ClamD protocol servers, and packages the bootable JAR. This slice adds six application tests, three MinIO signed-access scenarios, two PostgreSQL evidence/RLS scenarios, and one production-preflight test. A focused 49-test run and the clean full suite pass. Both Compose models resolve. The unchanged 15-operation/seven-convention API verifier and all seven negative tests, the 79-screen register, and the CI-security verifier with all ten negative tests were rerun and pass. Frontend source remains unchanged at its latest verified 29 unit tests and 18 desktop/mobile browser tests.

This closes only internal signed-access mechanics. The adapter and synthetic purpose/TTL policy remain disabled by default, bearer URLs remain memory-only, and no route can request or return one. Phase 0V later adds immutable retention/legal-hold-enablement mechanics; production still needs an approved M7 document/provenance state machine, permission and purpose mapping, governed read/audit/hold-release/disposal workflows, approved retention schedules, object-store IAM/KMS/Object-Lock/backup/monitoring acceptance, revocation/incident procedures, and protected HTTP/browser attack coverage.

## Phase 0V progress - immutable document retention and legal-hold mechanics resolved

The twenty-second implementation slice was completed and locally verified on 15 September 2026.

| Initial finding                                                                        | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Current state                                                                                                                                                                                                                                                                                 |
| -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A raw retention deadline or hold flag could otherwise reach storage                    | `DocumentRetentionOperations` accepts an authorized tenant context and a policy-keyed directive, requires committed V10 promotion evidence, checks tenant/purpose/policy, reloads the latest V12 retention state, and constructs a short-lived bounded authorization. Exact directive replay returns durable evidence without another storage call.                                                                                                                                      | PASS in seven application ordering, replay, denial, monotonicity, storage-failure, provider-drift, and evidence-drift tests; no HTTP route or production permission is added                                                                                                                  |
| Retention could be shortened, a legal hold released, or the wrong clean version locked | The opt-in S3-compatible adapter requires the existing private clean bucket to have versioning and Object Lock enabled, resolves the exact current provider version, revalidates metadata/type/size/digest/ETag, and performs a version- and ETag-bound full SHA-256 download before applying only S3 Object Lock `COMPLIANCE` retention and optionally enabling legal hold. It never uses governance bypass, shortens retention, disables a hold, creates a bucket, or deletes content. | PASS against pinned MinIO for first application, exact replay, retention extension, hold enablement, exact-version delete rejection, release refusal, corrupt/current-version drift, tenant/purpose/policy/age rejection, and activation failure without a pre-provisioned Object Lock bucket |
| Provider retention state had no durable tenant-safe proof                              | Flyway V12 adds append-only `document_retention_evidence`, linked to exact V10 promotion evidence and, for extensions, the exact prior V12 directive. It snapshots the policy, deadline, hold, purpose, authorization time, PostgreSQL application time, and only a SHA-256 hash of the provider storage version; bucket, object key, and raw provider version are absent.                                                                                                               | PASS for exact replay, latest-state lookup, policy snapshot, storage-identifier absence, tenant invisibility, restricted grants, forced RLS, and owner-level mutation attacks                                                                                                                 |
| Concurrent or forged evidence could bypass monotonic retention rules                   | The V12 insert trigger takes a per-document advisory transaction lock, binds request context and database time, rechecks promotion, purpose, policy bounds, authorization freshness, exact predecessor lineage, no shortening, no hold release, and no no-op directive. The application and storage adapter independently enforce the same monotonic boundary.                                                                                                                           | PASS for direct-SQL unpromoted, stale, unapproved-purpose, shortening, hold-release, stale-predecessor, conflict, and cross-tenant cases                                                                                                                                                      |
| Enabling object storage could accidentally enable retention                            | `careos.documents.retention.enabled` defaults to false and requires private S3 storage, runtime bucket creation disabled, an already versioned/Object-Lock-enabled clean bucket, and an explicit policy key, purpose allow-list, one-second-to-100-year retention bounds, one-second-to-five-minute authorization age, and zero-to-one-minute future skew. Production preflight and the document ArchUnit boundary cover activation and low-level port access.                           | PASS in Spring capability replacement, boundary-value, invalid-bucket, production-preflight, and architecture coverage                                                                                                                                                                        |

Local evidence: a clean Java 25 build compiles 179 production sources and 17 test sources, applies all twelve migrations to disposable PostgreSQL 18, passes all 144 backend tests across PostgreSQL, Redis, pinned Object-Lock-capable storage, and deterministic ClamD fixtures with zero failures, errors, or skips, and packages the bootable JAR. This slice adds seven application tests, three MinIO retention scenarios, two PostgreSQL retention/RLS scenarios, and one production-preflight test. The focused retention/security run covers 56 tests. Both Compose models resolve. The unchanged 15-operation/seven-convention API verifier and all seven negative tests, the 79-screen register, and the CI-security verifier with all ten negative tests were rerun and pass. Frontend source remains unchanged at its latest verified 29 unit tests and 18 desktop/mobile browser tests.

This closes only disabled-by-default immutable retention application and legal-hold enablement mechanics. It deliberately does not invent jurisdictional retention schedules, authorize a directive, expose a route, release a hold, shorten retention, dispose of an object, or add the M7 document/provenance lifecycle. Production still requires approved policy/permission/event semantics, governed disposal and hold-release approval/evidence, provider IAM/KMS/Object-Lock/backup/monitoring acceptance, recovery procedures, and protected HTTP/browser attack coverage.

## Phase 0W progress - UUIDv7 identifier strategy resolved

The twenty-third implementation slice was completed and locally verified on 15 September 2026.

| Initial finding                                                                                                                       | Implemented resolution                                                                                                                                                                                                                                                                                                                              | Current state                                                                                                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| New identifiers were generated with UUIDv4 independently in Java and PostgreSQL despite the specification requiring a UUIDv7 strategy | `UuidV7Generator` now provides RFC 9562 version/variant layout, a 48-bit Unix-millisecond timestamp, cryptographically secure random state, and synchronized process-local monotonic advancement for equal timestamps or clock rollback. Every production Java call site that previously invoked `UUID.randomUUID()` now uses the shared generator. | PASS for timestamp encoding, version/variant, 10,001 unique strictly ordered values in one millisecond, and clock rollback; direct production `UUID.randomUUID()` calls are absent |
| Database-generated identifiers still used `gen_random_uuid()`                                                                         | Flyway V13 requires PostgreSQL 18 and changes the defaults on all 12 active database-generated identifier columns to native `uuidv7()`. It deliberately neither rewrites existing rows nor constrains caller-supplied/historical UUIDs.                                                                                                             | PASS on a fresh PostgreSQL 18 database: catalog inspection proves the exact 12 defaults and a generated user identifier is version 7 with the RFC variant                          |
| Future code could silently reintroduce a separate UUIDv4 generation path                                                              | An eleventh ArchUnit rule prohibits production calls to `UUID.randomUUID()`, while the database integration suite locks the expected default-column set. Security guidance also forbids treating the embedded timestamp or lexical order as authorization, chronology evidence, or a substitute for server-owned timestamps.                        | PASS in the focused 15-test generator/architecture run, focused 26-test PostgreSQL run, and full clean suite                                                                       |

Local evidence: a clean Java 25 build compiles 180 production sources and 18 test sources, applies all thirteen migrations to disposable PostgreSQL 18, passes all 150 backend tests with zero failures, errors, or skips across PostgreSQL, Redis, pinned Object-Lock-capable storage, and deterministic ClamD fixtures, and packages the bootable JAR. This slice adds four generator tests, one architecture rule, and one PostgreSQL default test. Both Compose models resolve. The unchanged 15-operation/seven-convention API verifier and all seven negative tests, the 79-screen register, and the CI-security verifier with all ten negative tests were rerun and pass. Frontend source remains unchanged at its latest verified 29 unit tests and 18 desktop/mobile browser tests.

This closes generation strategy for current foundation identifiers only. Existing/reference UUIDs remain valid, UUID timestamps are observable rather than secret, and identifiers remain opaque references—not authorization or trusted event time. Every future table and production module must use the shared generator or an explicitly verified PostgreSQL UUIDv7 default while retaining server-owned timestamps, tenant context, and normal integrity controls.

## Phase 0X progress - reference authorization and governed identity administration resolved

The twenty-fourth implementation slice was completed and locally verified on 15 September 2026.

| Initial finding                                                                               | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                                              | Current state                                                                                                                                 |
| --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| Canonical policy was absent, so safe identity-administration paths could not execute          | V14 added four interactive reference roles, seven permissions, operation risk/denial/reason/recent-auth requirements, explicit delegation ceilings, a membership-role foreign key, and a database trigger that prevents removal or expiry of the final effective owner. V20 later promotes the approved replacement while retaining reference history.                                                              | Reference checkpoint PASS; production policy gap resolved in Phase 1AB                                                                        |
| Invitations were only a tenant-isolated table                                                 | V15 adds operation-to-event mappings and a complete issue/revoke/accept lifecycle with one pending invitation per tenant/email, delegable-role enforcement, one-use peppered token indexing, expiry, atomic existing-account linkage or new-account creation, exact idempotency, notification, audit, and outbox evidence. The administrator never receives the raw invitation token.                               | PASS in HTTP, database, replay, expiry, cross-tenant, and linkage attacks; production policy and delivery acceptance remain open              |
| Human sessions could be reused accidentally for background work                               | V16 adds five non-interactive reference roles, tenant-scoped service identities and bounded expiring credential records, user/service namespace separation, rejection of service roles on human memberships, and `ServiceIdentityAuthorizationOperations` for an exact credential digest, tenant, purpose, role, and operation under row locks.                                                                     | PASS for disjoint reference authorization; no provisioning API, production credential, worker, or scheduler is activated                      |
| Administrator MFA reset lacked independent approval and durable evidence                      | V17 maps request/approval/execution operations to an append-only tenant approval record. Target, maker, and checker separation is database-enforced; only the original maker may consume an unexpired approval with the exact target/reason/idempotency key. Execution revokes TOTP, recovery codes, session metadata/cache, emits authentication plus audit/outbox evidence, and notifies only on first execution. | PASS across controller and direct-database attacks, exact replay, expiry replacement, wrong-actor/target/reason, and evidence-tampering cases |
| Browser identity administration was still a placeholder                                       | M1-02 now drives governed issue/revoke and public one-use acceptance/linkage. M1-03 drives all three administrative-reset states with caller-owned retry keys, recent authentication, runtime response validation, safe correlation references, and no persisted secret/token material.                                                                                                                             | PASS in 35 unit and 20 desktop/mobile browser/Axe tests; remaining business screens are synthetic                                             |
| New cross-module calls introduced an architecture cycle and E2E could reuse an unrelated port | Tenancy now depends only on a shared authenticated-actor contract, restoring module cycle freedom. Playwright starts CareOS on a dedicated strict port and refuses arbitrary server reuse.                                                                                                                                                                                                                          | PASS in all 11 ArchUnit rules and all 20 browser cases                                                                                        |

Local evidence: `./mvnw -B -ntp clean verify` on Java 25 compiles 210 production sources and 18 test sources, applies all 17 migrations, passes all 162 tests with zero failures/errors/skips, and packages the JAR. OpenAPI 3.1 version 0.8.0 verifies all 21 operations and seven conventions; all seven API negative tests pass. Frontend generation, architecture/four negative fixtures, strict typecheck, lint, formatting, all 35 unit tests, production build, and all 20 browser tests pass. The 79-screen register, both Compose models, the CI-security verifier, and all ten CI-security negative tests pass. Both current images build; an isolated hardened smoke reaches backend readiness at Flyway V17 and serves/proxies the frontend successfully under the declared non-root/read-only/capability restrictions.

This completes the repository-level Phase 0 reference implementation, not production acceptance. Phase 1AB later activates the approved replacement policy and Phase 1AG later enforces its mandatory-role MFA/self-disable rules. Optional viewer/editor organization-policy behavior, service-credential provisioning and rotation, real workers/providers, infrastructure/security operations, and business-module behavior remain unavailable until separately supplied and approved.

## Phase 1R progress - provisional organization core reference slice resolved

The first executable Module 1 reference slice was completed and locally verified on 16 September 2026.

| Initial finding                                                                 | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                    | Current state                                                                                                                                             |
| ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1-05 and M1-06 displayed browser-authored readiness and counts                 | The administration application service calculates bounded counts and a documented gate subset inside an authorized read-only tenant transaction. The dashboard and checklist consume the same checked server response and reject tenant/count/gate inconsistencies at runtime.                                                                                                            | Engineering boundary PASS; approved full gate catalogue still requires M1C implementation                                                                 |
| M1-07 was a local-only form with no concurrency or durable business state       | Flyway V18 constrains the existing organization aggregate and records updater/revision evidence. The protected profile endpoint returns a strong ETag; updates require exact `If-Match`, a caller-owned idempotency key, an explicit reason, bounded field validation, and a real IANA timezone.                                                                                          | PASS for successful update, exact replay, changed-payload conflict, stale precondition, missing precondition, validation, and direct-SQL boundary attacks |
| A business mutation could otherwise bypass policy or commit incomplete evidence | The new application path enters `TenantAuthorizationOperations`, executes through `GovernedMutationExecutor`, and atomically commits the profile revision, one append-only audit record, one outbox record, and the idempotency response. V18 limits runtime organization updates to the exact reference operation, actor, tenant, reason, mutable field set, and monotonic lock version. | PASS with reference RBAC/RLS/trigger enforcement; production preflight continues to reject the reference registry                                         |
| The frontend had no bounded business feature                                    | `features/administration` now owns real M1-05/M1-06/M1-07 states and is composed by the root without cross-feature imports. It handles selected-organization changes, loading/failure/retry, validation, success, and stale-edit recovery while retaining a stable retry key across uncertain failures.                                                                                   | PASS in architecture, type, lint, formatting, 40 unit, and 22 desktop/mobile Playwright/Axe tests                                                         |
| The checked contract ended at identity administration                           | OpenAPI 3.1 version 0.9.0 adds readiness read, profile read, and profile update, bringing the generated checked client to 24 operations. Contract verification requires tenant-prefixed paths, CSRF/correlation/Problem behavior, strong concurrency, idempotency, and caller-controlled retry semantics.                                                                                 | PASS across all seven conventions and seven negative contract tests                                                                                       |

Local evidence: a clean Maven 3.9.11/Java 25 build compiles 219 production sources and 18 test sources, applies all 18 migrations to disposable PostgreSQL 18, passes all 164 backend tests with zero failures, errors, or skips, and packages the bootable JAR. The focused HTTP and direct PostgreSQL organization-core tests also pass independently. OpenAPI version 0.9.0 verifies all 24 operations and seven conventions; all seven API negative tests pass. Frontend API generation, two-feature architecture/four negative fixtures, strict typecheck, lint, formatting, all 40 unit tests, production build, and all 22 browser tests pass. The 79-screen register, both Compose models, the CI-security verifier/all ten negative tests, and both current application image builds pass.

This slice was explicitly a reference implementation, not M1C production completion. Phase 1AA later approves its governing inputs and V20 promotes implemented bindings, but the full approved profile/readiness catalogue, final UI, M1-08 through M1-23 workflows, and M1C acceptance remain incomplete.

## Phase 1S progress - current-image assurance resolved

The current V18 application images and complete default Compose topology were locally revalidated on 16 September 2026.

| Initial finding                                                         | Implemented resolution                                                                                                                                                                                                                                                              | Current state                                                                                                         |
| ----------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| The official PostgreSQL 18 image rejected the legacy data-volume target | Compose now mounts the named database volume at the supported `/var/lib/postgresql` major-version parent. The dependency-free security verifier rejects a PostgreSQL 18 service that reintroduces `/var/lib/postgresql/data`, and its existing mutation fixture covers the failure. | PASS in both Compose models, all ten security-contract tests, and fresh six-service initialization                    |
| Current V18 images had build evidence but only the older V17 smoke      | A fresh isolated stack proves Flyway V18, backend/frontend health, one synthetic organization, frontend security headers and 79-screen API proxying, protected metrics, non-root users, read-only roots, writable `/tmp`, dropped capabilities, and no-new-privileges.              | PASS; every temporary container, network, volume, image, and scanner cache was removed                                |
| Security scans and SBOMs were recorded only at an earlier checkpoint    | Trivy 0.74 reran the source and exact-image HIGH/CRITICAL gates with unfixed findings ignored, checked both Dockerfiles, and generated parseable CycloneDX 1.7 inventories for the current images.                                                                                  | PASS with zero fixed HIGH/CRITICAL findings, zero Dockerfile misconfigurations, and no emitted secret finding locally |

This closes only the locally executable assurance gap. Hosted checks, durable SBOM retention, artifact signing/provenance, registry admission, deployment verification, production infrastructure acceptance, and the Module 1 owner-input gate remain open.

## Phase 1T progress - executable Module 1 input gate resolved

The documented Module 1 approval boundary is now machine-readable and fail-closed without pretending the required product evidence exists.

| Initial finding                                             | Implemented resolution                                                                                                                                                                                                                                                                                                     | Current state                                                                                               |
| ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `BLOCKED_INPUT` existed only as prose                       | A checked JSON Schema and state manifest enumerate the exact eight artifact bundles. The dependency-free verifier reports present/missing evidence and validates bounded versions, repository-contained regular files, lowercase SHA-256 content, category/path uniqueness, and package completeness.                      | Gate mechanics PASS; checked state later advanced to approved in Phase 1AA                                  |
| An approval label could be detached from the supplied files | Approved state requires all eight artifacts and binds their canonical descriptors into one deterministic package SHA-256. The record must identify a non-placeholder authority, a bounded record ID, a non-future UTC time, the ordered M1-01 through M1-23 scope, and a distinct checksum-matched approval-evidence file. | PASS across fourteen focused positive/negative tests; no real approval exists                               |
| CI could otherwise stop checking the gate silently          | The quality workflow and project verifier run the gate and its test suite. The CI-security contract requires both workflow commands, increasing its current attack suite from ten to eleven tests.                                                                                                                         | PASS locally; `--require-approved` deliberately exits nonzero while the checked state remains input-blocked |

This tooling validates future evidence; it does not supply mockups, design assets, policy decisions, data/lifecycle catalogues, event schemas, export rules, or owner authority. The production Module 1 checklist remains fully open.

## Phase 1U progress - environment-scoped foundation seed strategy resolved

The historical V1 fixture is now prevented from becoming default or production runtime tenant data without rewriting an applied migration.

| Initial finding                                                                 | Implemented resolution                                                                                                                                                                                                                                        | Current state                                                                                      |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| V1 unconditionally inserted one synthetic organization and facility             | V19 removes the exact reserved rows whenever the base or production configuration is used. Only the hard-coded `local` and `test` Flyway placeholders retain them, and the production value cannot inherit the local choice.                                  | PASS: a fresh production/default migration reaches V19 with zero organizations and zero facilities |
| A compensating cleanup could destroy a fixture that had become real tenant data | V19 verifies the exact unchanged organization/profile and facility, rejects additional facilities, and translates any foreign-key dependency into an explicit remediation failure. The migration is transactional and never uses a cascade.                   | PASS: a changed facility rolls the migration back, records no V19 success, and preserves both rows |
| Configuration or migration drift could silently restore production fixtures     | The dependency-free security contract requires base/production false, local/test true, the scoped placeholder, both cleanup statements, and foreign-key fail-closed handling. A mutation test rejects weakened production configuration and cleanup behavior. | PASS across the repository verifier and all twelve security-contract tests                         |
| The two modes lacked executable database evidence                               | A dedicated PostgreSQL 18 test migrates through V18, proves unsafe V19 rollback, explicitly restores the fixture, retries V19, and proves removal. The existing test-profile RLS suite independently reaches V19 while retaining its isolated fixture.        | PASS in the focused migration test, tenant-RLS suite, and complete 165-test Maven build            |

This resolves seed scoping, not tenant provisioning. Production organization creation, initial-owner ceremony, approved activation policy, import/migration tooling, and operator runbooks remain business-module and deployment work.

## Phase 1V progress - fail-closed protected route resolution resolved

The provisional hash router no longer treats an unregistered or malformed protected route as the administration dashboard.

| Initial finding                                                                       | Implemented resolution                                                                                                                                                                                                                                  | Current state                                                                                                              |
| ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| An unknown protected hash silently resolved to M1-05                                  | The root now admits only IDs in the checked 79-screen registry after authentication and organization selection. The registry lookup itself throws on an unknown ID instead of returning a default screen.                                               | PASS: an authenticated unknown route renders no business page or synthetic dashboard                                       |
| Reusing the shell for an invalid route could incorrectly mark a default screen active | The shell now supports an intentionally absent current screen, retains authenticated organization/sign-out controls, selects only the safe M1 workspace navigation context, and marks no screen link active.                                            | PASS in strict type, lint, architecture, and component coverage                                                            |
| The missing-route state lacked accessible recovery and responsive proof               | A dedicated page moves focus to its heading, does not reflect attacker-controlled route text, states that no business screen loaded, and offers one explicit link to M1-05. Playwright checks recovery and Axe on desktop and the 320px mobile project. | PASS in 42 unit tests and 24 desktop/mobile browser tests, including two new unit cases and two project-specific E2E cases |

This is route-boundary hardening, not an approved production route design. The application still uses hash navigation, the 72 generic protected entries remain synthetic, and replacement with the approved route hierarchy and screen-specific business states remains input-blocked.

## Phase 1W progress - exact responsive-breakpoint assurance resolved

The current reference UI is now exercised at every viewport width required by the specification, and the expanded matrix exposed and fixed a real narrow-layout defect.

| Initial finding                                                                        | Implemented resolution                                                                                                                                                                                                                                                       | Current state                                                                                                              |
| -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Browser coverage existed only at a default desktop width and 320 pixels                | Playwright now declares exact 1440, 1024, 768, 390, and 320 pixel projects. The two narrow projects carry mobile/touch semantics, while 768 verifies the persistent-navigation boundary immediately above the 760px drawer rule.                                             | PASS: all five named projects exercise the complete browser workflow set                                                   |
| The route sweep checked visibility and Axe but could miss off-screen document overflow | Every registered route now compares document and body scroll widths with the viewport and reports bounded offending-element diagnostics. The sweep retains route-specific Axe checks and uses a two-minute timeout only for the enlarged multi-route cases.                  | PASS across 395 registered-route viewport renders plus identity, recovery, invitation, MFA, profile, and session workflows |
| List tables overflowed the page at 768, 390, and 320 pixels                            | The table scroll wrapper now establishes a positioned, zero-minimum-width, 100%-bounded containing block. This keeps the absolutely positioned screen-reader-only Actions label inside the horizontal scroller instead of extending the document to the table's 740px width. | PASS: the original M1-12 and M2-02 failures and the complete five-width matrix are green                                   |
| Responsive navigation behavior was implicit                                            | The browser suite asserts persistent navigation and a hidden menu at 1440/1024/768, drawer opening/closing at 390/320, exact configured widths, successful workspace navigation, and no resulting document overflow.                                                         | PASS in five dedicated navigation cases                                                                                    |
| Removing one viewport project could silently reduce the matrix                         | The dependency-free repository verifier now requires each exact named project/device contract plus document/body, registered-route, identity-route, exact-width, and drawer-boundary assertions. A mutation test removes 768 coverage and weakens overflow detection.        | PASS in the verifier and all thirteen CI-security contract tests                                                           |

This verifies containment and accessibility of the current reference UI, not owner acceptance of responsive product designs. Approved mockups, Design System breakpoint behavior, screen-specific card/table alternatives, complete keyboard/dialog focus workflows, and WCAG 2.2 AA acceptance remain production-input work.

## Phase 1X progress - honest synthetic-interaction containment resolved

The generic routes now communicate their non-production boundary before users reach any sample content and no longer make a no-op or browser-local transition look like a persisted business action.

| Initial finding                                                                                        | Implemented resolution                                                                                                                                                                                                                                                                                                                                                           | Current state                                                            |
| ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| Generic records and clinical identities could be mistaken for real data                                | Every generic page now exposes a named synthetic-prototype notice that says values are illustrative, production actions are unavailable, and real personal or clinical information must not be entered. Person, organization, readiness, and clinical labels were replaced with unambiguous synthetic samples; generic form and clinical fields are read-only/disabled previews. | PASS across dashboard, list, form, and clinical templates                |
| `Review`, `Open`, form saves, and clinical save/confirmation either did nothing or changed local state | The fake local save/confirmation transitions were removed. Each unimplemented workflow action is now a natively disabled button with an action-specific accessible name, a shared explanatory description, and no navigation, mutation, or success claim.                                                                                                                        | PASS: no generic control represents an unapproved workflow as successful |
| List row names navigated to unrelated generic routes and `Clear filters` was inert                     | Misleading record links were replaced with text, `Open` is explicitly unavailable, and search/status/scope are controlled filters over the visible synthetic rows. `Clear filters` is disabled when unnecessary and resets every filter when active; a polite result summary reports the bounded local result count.                                                             | PASS in component and all-five-width browser interaction coverage        |
| Terminal pagination used a focusable anchor with `aria-disabled` and could still activate              | Pagination endpoints now render non-link disabled text; only valid previous/next destinations render anchors.                                                                                                                                                                                                                                                                    | PASS for the COS-27 terminal boundary                                    |
| Removing fake row links left narrow horizontal table scrollers unreachable by keyboard                 | The complete Axe matrix detected the regression. Each table scroller is now a named, focusable region with an explicit focus ring, preserving keyboard horizontal scrolling without restoring a misleading action.                                                                                                                                                               | PASS across all M1/M2 list routes at 1440/1024/768/390/320               |

Local evidence: strict typecheck, lint, formatting, all 45 frontend unit tests, and the production build pass. All 65 Playwright cases pass across the exact five-project matrix, including a new interaction-boundary case in every project, 395 registered-route viewport renders, serious/critical Axe rejection, and document/body overflow checks. Backend, database, and API behavior are unchanged from the Phase 1U baseline.

This completes honest containment of the synthetic reference pages, not any production workflow. The disabled controls must be replaced only by approved, authorized, tenant-safe, persisted, audited, and tested server behavior after the relevant input gate passes.

## Phase 1Y progress - Module 1 owner-review draft packet resolved

The eight missing decision categories now have a source-grounded review starting point without fabricating approval. The packet is deliberately separate from `approved-inputs/module-1/`, labels every document `DRAFT_NOT_APPROVED`, identifies the owner decisions still required, and provides an acceptance checklist for each category.

| Initial finding                                                          | Implemented resolution                                                                                                                                                                                                                           | Current state                                                                             |
| ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- |
| Owners had an eight-category gate but no consolidated material to review | `docs/module-1-review-drafts/` now contains briefs for screen mockups, Design System, data/validation, lifecycle/transitions, authorization, audit/events, readiness/activation, and history/export, plus a handoff index.                       | PASS as review preparation only; none of the eight files is approved evidence             |
| Reference behavior and missing product decisions could be confused       | Every brief separates repository evidence and proposed review baselines from explicit owner decisions and acceptance criteria. Candidate lifecycle, permission, event, readiness, retention, and interaction choices remain visibly non-binding. | PASS: the packet supports review without promoting reference policy                       |
| A draft could be relabelled or moved into the production gate unnoticed  | A dependency-free verifier requires the exact eight canonical categories, paths, order, draft metadata, required sections, all 23 M1 screen IDs, regular non-symbolic files, and the `DRAFT_NOT_APPROVED` status.                                | PASS with 8/8 drafts, deterministic package digest, and `implementationAuthorized: false` |
| Review-package checking was not part of normal repository assurance      | The quality workflow, local verification script, and repository security contract now run both the draft verifier and its seven negative/positive tests.                                                                                         | PASS in the draft suite and all thirteen CI-security contract tests                       |

At this historical checkpoint the production manifest remained `BLOCKED_INPUT`. Phase 1AA records the later unchanged acceptance and exact approved promotion; the draft packet itself remains non-authorizing.

## Phase 1Z progress - concrete Module 1 candidate input package resolved

The review baselines are now expressed as one internally consistent `m1-candidate-1` package under `candidate-inputs/module-1/`. It resolves the prior open questions into reviewable working decisions without claiming owner acceptance or enabling a production registry, API, migration, worker, or screen.

| Initial finding                                                                                      | Implemented resolution                                                                                                                                                                                                                                                    | Current state                                                                                      |
| ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| The review packet described required mockups but did not contain a navigable all-screen visual       | A self-contained HTML candidate provides keyboard-operable navigation for M1-01 through M1-23, state switching, operation/permission/assurance annotations, responsive table/card behavior, dialogs, focus recovery, and exact 1440/1024/768/390/320 rules.               | PASS in five Playwright/Axe/overflow cases; it makes no network request and persists no data       |
| Data, lifecycle, authorization, event, readiness, and export decisions still remained open questions | Seven versioned candidate documents now fix field bounds/classifications, state transitions, role grants/assurance, event/payload/consumer mappings, deterministic readiness gates/timing, Design System tokens/components, and purpose-bound history/export controls.    | PASS as a bounded implementation candidate; still subject to accountable review                    |
| A candidate could be confused with either drafts or approved production evidence                     | `contracts/module-1-candidate-inputs.json` uses `CANDIDATE_FOR_APPROVAL`; a dependency-free verifier enforces eight exact artifacts, candidate-only paths/version, non-approval metadata, required decisions, all 23 screens, and offline/non-persistent mockup behavior. | PASS at digest `c2087548aacd35eb4927532d63b844851c6fe9c56cccb56e46596d707d7a1a53`, authority false |
| Candidate integrity could disappear from routine quality checks                                      | The verifier and eight positive/negative tests run in local/CI quality; the repository security contract rejects removal. The complete browser suite now contains 70 cases, including the five new candidate viewport cases.                                              | PASS; all 8 candidate tests, 13 repository-security tests, and 70 browser tests pass               |

This was the final preapproval state. The candidate was subsequently approved unchanged and promoted through the independently verified production gate below; its source directory remains non-authorizing provenance.

## Phase 1AA progress - exact Module 1 input approval resolved

| Initial finding                                                                          | Implemented resolution                                                                                                                                                                                                                                      | Current state                                                                                        |
| ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Production contained no approved Module 1 implementation package                         | The exact eight `m1-candidate-1` files were copied byte-for-byte to `approved-inputs/module-1/`; no accepted design or policy byte was silently changed.                                                                                                    | PASS: all eight checksums validate                                                                   |
| Candidate integrity did not itself provide accountable implementation authority          | `M1-APPROVAL-20260916-01` records approval by **bhupendra, developer**, all M1-01 through M1-23 screens, decision time, candidate/package/evidence digests, and an explicit unchanged disposition.                                                          | PASS at production package digest `19aff5ce30516b7ee2101c093a8429d8a74394995ca90d486790bcc18a392946` |
| The checked production manifest truthfully remained blocked before a real record existed | The manifest now reports `APPROVED`, 8/8, no missing artifacts, and `implementationAuthorized: true`; normal and `--require-approved` verification both pass. Existing blocked/partial/tampered/placeholder/future cases remain isolated negative fixtures. | PASS in all 14 input-gate tests                                                                      |

Approval clears `BLOCKED_INPUT`; it does not complete a delivery slice. Any checksum drift requires a new accountable approval rather than editing the current record in place.

## Phase 1AB progress - approved authorization release and first M1B increment resolved

| Initial finding                                                                      | Implemented resolution                                                                                                                                                                                                                  | Current state                                                                                                           |
| ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Approved roles, permissions, grants, delegations, and release evidence were not live | Flyway V20 adds immutable checksum-bound `authorization_registry_releases`, activates the exact approved interactive catalogue, and leaves `local_bootstrap`, obsolete member policy, and non-interactive service roles reference-only. | PASS through fresh PostgreSQL 18 migration and catalog/immutability assertions                                          |
| Existing invitation and MFA-reset operations still used provisional bindings         | Implemented operations/events now use approved `identity.invitation.*`, `access.invitation.*`, and `access.mfa_reset.*` bindings; the old MFA target workflow is retired while its evidence remains.                                    | PASS through the complete 14-case identity HTTP flow and database lifecycle coverage                                    |
| High-risk operations checked recent authentication but not separate recent MFA proof | Tenant authorization now reads `mfa_required`, validates a distinct bounded session MFA timestamp, and emits fail-closed `428 mfa-required`; invitation issue/revoke and MFA reset request/approve/execute enable it.                   | PASS, including explicit missing-MFA denial                                                                             |
| Production preflight rejected identity administration unconditionally                | Invitation/MFA activation remains off by default and is allowed only for exact registry `m1-candidate-1` plus approved package digest; registry or digest drift fails startup, and the reference-policy flag remains forbidden.         | PASS in focused production-guard coverage                                                                               |
| M1-02 exposed obsolete reference roles                                               | The UI defaults to `organization_viewer` and offers the approved non-owner roles; server delegation remains authoritative. OpenAPI descriptions and generated types state the added MFA assurance.                                      | PASS in API drift, architecture, format, typecheck, lint, 45 unit tests, production build, and all 70 browser/Axe cases |

At this checkpoint M1B remained open for M1-20 membership list/read, mutations, owner transfer, action projection, and acceptance. The following Phase 1AC increment resolves the list/read and action-projection portion only.

## Phase 1AC progress - authorized M1-20 membership read resolved

| Initial finding                                                             | Implemented resolution                                                                                                                                                                                                                                                                                                                                                              | Current state                                                                                                |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| M1-20 used synthetic membership rows and had no approved read operation     | Flyway V21 activates exact operation `access.membership.read`; `GET /api/v1/organizations/{organizationId}/memberships` authorizes inside the tenant transaction, applies forced RLS, uses hidden denial, and returns only the approved minimum membership projection.                                                                                                              | PASS through fresh PostgreSQL 18 migration, HTTP authorization, and direct tenant-bound persistence coverage |
| Search, filters, and pagination had no enforceable server contract          | The endpoint NFC-normalizes literal search, enforces the approved 2-100 character bound and allow-listed access-state/role filters, rejects unknown or duplicate parameters, and uses deterministic descending keyset paging. Opaque cursors are HMAC-SHA256 signed, expire after 15 minutes, and bind operation, tenant, normalized filters, limit, snapshot, and keyset position. | PASS for valid paging plus tamper and filter-rebinding rejection                                             |
| Browser state could otherwise infer administrative actions                  | The read projection computes actions from current live approved permissions. Page action `issueInvitation` and eligible-row action `requestMfaReset` are returned only when authorized; self/inactive/non-MFA targets are excluded, and role/scope/revoke/owner actions are not advertised before their workflows exist.                                                            | PASS without trusting client role names or cursor-time permission state                                      |
| The generic screen and 24-operation contract did not describe the real flow | OpenAPI 3.1 version 0.10.0 defines operation 25 and its page/row schemas; generated types/client are drift-checked. M1-20 now renders runtime-validated loading, failure/retry, empty/no-result, filter, cursor-history, responsive table, and permission-projected navigation states.                                                                                              | PASS in 47 frontend unit tests and five new M1-20 Playwright/Axe cases, within the full 75-case matrix       |

Local evidence: a clean Maven 3.9.11/Java 25 build compiles 223 production sources and 19 test sources, applies all 21 migrations to disposable PostgreSQL 18, passes all 168 backend tests with zero failures, errors, or skips, enforces all 11 architecture rules, and packages the bootable JAR. OpenAPI 0.10.0 verifies 25 operations and seven conventions; all seven negative tests pass. Frontend API drift, the 21-source/48-import architecture boundary and four negative fixtures, strict typecheck, lint, formatting, all 47 unit tests, the production build, and all 75 browser tests across 1440/1024/768/390/320 pass. The 79-screen register, both Module 1 approval modes and 14 tests, review/candidate gates, and all 13 repository-security tests also pass.

At that checkpoint M1B remained open for maker-checker membership role/scope change and revocation, final-owner-safe owner transfer, approved final visual acceptance, complete mutation attack/browser regression, target-environment evidence, and owner acceptance. The following Phase 1AD increment resolves organization-wide non-owner role changes and revocation only.

## Phase 1AD progress - governed non-owner membership changes resolved

| Initial finding                                                                    | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                 | Current state                                                                                                             |
| ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| M1-20 could display members but could not govern a membership change               | Flyway V22 activates exact approved `access.membership.change.request`, `access.membership.change.approve`, and `access.membership.change`; immutable forced-RLS requests bind tenant, target membership/user, from/to role, change type, revision, maker, reason, approval, expiry, and outcome.                                                                                      | PASS through fresh PostgreSQL 18 migration, API flows, and direct database-boundary attacks                               |
| A client or runtime role could otherwise bypass approval or mutate stale authority | Strong membership ETags and lock versions reject missing/stale preconditions; PostgreSQL permits only the exact approved role change or active-to-revoked transition while consuming the exact live approval. It independently enforces non-owner/self/target restrictions, maker/checker separation, delegation ceilings, final-owner safety, reason equality, and execution context. | PASS for `428`, `412`, hidden denial, wrong executor, replay, stale revision, and unapproved direct-SQL mutation cases    |
| High-risk membership changes lacked assurance and evidence                         | Request/approve/execute require recent primary authentication plus recent MFA, 10-500 NFC/control-free reasons, caller-owned idempotency, and no-store responses. Final approved `identity.membership.changed` or `identity.membership.revoked` audit/outbox evidence is atomic with the membership update.                                                                            | PASS for exact independent approval, role change, revocation, six expected evidence records, and zero bypass side effects |
| M1-20 did not expose checked mutation states                                       | OpenAPI 3.1 version 0.11.0 adds three operations and checked schemas. Live action projection now returns eligible role-change/revocation row actions plus approval/execution page actions; the runtime-validated panel implements all three stages and refreshes the list after execution.                                                                                             | PASS in generated-client drift, transport/UI unit coverage, and five-viewport Axe/overflow browser coverage               |
| Unaccepted production activation could expose the new workflow                     | Membership administration is disabled by default and production activation is bound to the exact approved registry and package digest by the startup guard. Owner transfer and facility-scoped grants remain explicitly unavailable.                                                                                                                                                   | PASS in configuration binding and fail-closed UI/action boundaries                                                        |

Local evidence: a Maven 3.9.11/Java 25 verification build compiles 232 production sources and 19 test sources, validates and applies all 22 migrations to disposable PostgreSQL 18, passes all 170 backend tests with zero failures, errors, or skips, enforces all 11 architecture rules, and packages the bootable JAR. OpenAPI 0.11.0 verifies 28 operations and seven conventions. Frontend API drift, the 21-source/48-import architecture boundary and four negative fixtures, strict typecheck, lint, formatting, all 49 unit tests, the production build, and all 80 browser tests across 1440/1024/768/390/320 pass.

M1B remains open for facility-scoped membership grants/scope changes, final-owner-safe owner transfer, final visual/owner acceptance, target-environment evidence, and full slice acceptance.

## Phase 1AE progress - governed final-owner-safe owner transfer resolved

| Initial finding                                                                           | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                                          | Current state                                                                                                                            |
| ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Approved owner-transfer permissions and final event had no executable workflow            | Flyway V23 binds approved permissions `access.owner_transfer.request/approve/execute` to registry-valid `access.owner-transfer.request/approve/execute` operations, creates immutable forced-RLS `owner_transfer_requests`, and registers exact final `identity.owner.transferred` evidence.                                                                                                                    | PASS through fresh PostgreSQL 18 migration, runtime registry checks, and final audit/outbox assertions                                   |
| Owner role mutation could otherwise bypass independent approval or remove the final owner | PostgreSQL binds the exact organization, target membership/user, from/to role, transfer type, revision, maker, checker, approved maker-executor, reason, and 30-minute expiry. It permits only non-owner-to-owner promotion or owner-to-approved-non-owner demotion, requires an indefinite promotion target, applies the demotion delegation ceiling, and retains the existing final indefinite owner trigger. | PASS for maker/checker/target separation, wrong executor, stale revision, replay, last-owner demotion, and unapproved direct-SQL attacks |
| The API/UI could not request, approve, or execute an owner transition                     | Three checked endpoints enforce recent primary authentication plus MFA, strong membership ETags, caller-owned idempotency, hidden target denial, exact replay, no-store responses, and conflict-safe execution. Live action projection and the M1-20 governed panel expose eligible promote/demote, approve, and execute states.                                                                                | PASS in HTTP, generated-client transport, runtime UI validation, and five-viewport browser/Axe/overflow coverage                         |
| The checked contract and evidence snapshot stopped at non-owner changes                   | OpenAPI advances to version 0.12.0 with 31 operations and regenerated TypeScript artifacts. The final event carries only the approved six-key payload and commits atomically with the owner-role transition.                                                                                                                                                                                                    | PASS in contract/negative tests, client drift, event-schema checks, and idempotent execution evidence                                    |
| Facility scope could be inferred from one policy sentence                                 | The UI and server continue to omit facility-scope actions. The approved package says facility grants may narrow editor/viewer access, but defines no grant record, lifecycle, mutation contract, or exact enforcement semantics.                                                                                                                                                                                | OPEN pending an approved additive facility-grant contract; no policy was invented                                                        |

Local evidence: a Maven 3.9.11/Java 25 clean verification compiles 232 production sources and 19 test sources, validates and applies all 23 migrations to disposable PostgreSQL 18, passes all 171 backend tests with zero failures, errors, or skips, enforces all 11 architecture rules, and packages the bootable JAR. OpenAPI 0.12.0 verifies 31 operations and seven conventions. Frontend API drift, architecture, strict typecheck, lint, formatting, all 51 unit tests, the production build, and all 85 browser tests across 1440/1024/768/390/320 pass.

M1B remains open for approval and implementation of the facility-scoped membership grant/scope-change contract, final visual/owner acceptance, target-environment evidence, and full slice acceptance. Owner transfer is no longer an implementation gap.

## Phase 1AF progress - approval-ready facility-scope contract resolved

| Initial finding                                                                                                                         | Implemented resolution                                                                                                                                                                                                                                                                                                                            | Current state                                                                                 |
| --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| The approved policy allowed facility scope to narrow editor/viewer access but supplied no exact grant record or current-state invariant | Additive candidate `m1-facility-scope-candidate-1` defines explicit `organization`/`facilities` modes, eligible roles, temporal `access_assignments`, immutable request targets, canonical set digests, 1-to-100 bounds, same-tenant links, forced RLS, history, and deferred membership/assignment consistency.                                  | PASS as a candidate contract only; no production table or behavior exists                     |
| Scope enforcement and its interaction with other membership transitions were ambiguous                                                  | The candidate defines persisted resource-to-facility derivation, permission intersection, SQL list/count filtering, organization-wide denial families, immediate full-set replacement, and exact cleanup/preservation rules for role change, revocation, promotion, demotion, suspension, and closure.                                            | Exact proposal ready for accountable decision; runtime remains unchanged                      |
| A scope mutation could otherwise bypass the governed M1-20 workflow or invent new authority                                             | The candidate reuses approved `access.membership.change.request`, `.approve`, and `.change` operations, current owner grants, five-minute recent-authentication/MFA, reason, idempotency, strong revision, 30-minute approval, maker/checker/target separation, and original-maker execution.                                                     | No new grant or operation is proposed; approval is still required                             |
| Event detail and UI/API behavior were unspecified                                                                                       | The candidate reuses the exact six-key `identity.membership.changed` final event without facility IDs, retains detailed RLS evidence, extends the existing routes with a conditional request shape, and defines bounded membership projections, server-derived `requestScopeChange`, full-replacement UI, and five-viewport/accessibility states. | Contract is complete enough to implement after approval                                       |
| A prose proposal could drift or be mistaken for authority                                                                               | A strict verifier binds the one artifact to the approved base registry, approval record, and package digest; computes package SHA-256 `76a3f7a2c63cef0cab02b8a1d38a66220eee0abb99c9be0a82b64eaa8941fddb`; rejects false approval, path/basis/shape drift, missing decisions, and unresolved markers; and is mandatory in CI.                      | PASS across 7 candidate tests and 13 CI-security tests with `implementationAuthorized: false` |

This closes the missing-contract authoring task, not the facility-scope implementation task. An accountable approval record must bind the exact candidate version and digest before a migration, endpoint, permission action, or UI control is implemented.

## Phase 1AG progress - approved role-level MFA enforcement resolved

| Initial finding                                                                                | Implemented resolution                                                                                                                                                                                                                                                                                                                                                                                                      | Current state                                                                                                                                                |
| ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| The approved six-role MFA mandate was documented but not represented in executable role state  | Flyway V24 requires the exact active `m1-candidate-1` release, adds `authorization_roles.mfa_required`, and marks only `organization_owner`, `organization_administrator`, `configuration_approver`, `security_administrator`, `auditor`, and `export_approver`. `local_bootstrap`, reference roles, editor, and viewer remain nonrequired.                                                                                 | PASS through exact catalog assertions on fresh PostgreSQL 18                                                                                                 |
| Login could otherwise expose organization data before a mandatory-role account enrolled        | A non-enrolled mandatory account receives HTTP `202` and state `mfa_enrollment_required`. Its restricted authority can reach only session/logout and MFA enrollment/verification; organization loading and all other API access remain locked. Successful verification rotates the session, records primary/MFA evidence, and the browser withholds workspace bootstrap until the one-time recovery codes are acknowledged. | PASS in HTTP, session-authority, unit, and five-viewport browser tests                                                                                       |
| A role promotion could leave an already authenticated password-only session usable             | The session-validity filter reloads authoritative account/MFA requirements on every protected request and invalidates the stale session with `401 mfa-enrollment-required` as soon as a mandatory membership becomes effective.                                                                                                                                                                                             | PASS in role-promotion integration coverage                                                                                                                  |
| Enabled factors could be removed outside the approved loss-recovery path                       | A PostgreSQL trigger rejects every enabled-to-disabled/revoked MFA transition unless the exact `identity.mfa.admin-reset.execute` organization, actor, approval, target, correlation, consumed state, and executor are transaction-bound. No self-disable endpoint is exposed.                                                                                                                                              | PASS for direct-SQL rejection and the existing governed reset success path; optional-role self-disable remains unavailable pending a separate exact contract |
| Approved access readiness keys were still approximated                                         | Readiness now exposes exact `access.final_owner` and `access.mfa_enforced` gates. The former requires an active indefinite owner after subtracting pending open demotions; the latter requires MFA on every effective mandatory-role account and no expired approved reset.                                                                                                                                                 | PASS in readiness integration and frontend projection tests                                                                                                  |
| Session and generated-client contracts lacked the distinction between enabled and required MFA | OpenAPI advances to version 0.13.0; `SessionState` requires `mfaRequired` and includes `mfa_enrollment_required`. Runtime validators, generated types, the checked client, and session state machine use the distinction fail-closed.                                                                                                                                                                                       | PASS for all 31 operations, seven conventions, seven negative contract tests, and generated drift                                                            |

Local evidence: a Maven 3.9.11/Java 25 clean verification compiles 232 production sources and 19 test sources, validates and applies all 24 migrations to disposable PostgreSQL 18, passes all 174 backend tests with zero failures, errors, or skips, enforces all 11 architecture rules, and packages the bootable JAR. OpenAPI 0.13.0 verifies all 31 operations and seven conventions. Frontend API drift, the 21-source/48-import architecture boundary and four negative fixtures, strict typecheck, lint, formatting, all 52 unit tests, the production build, and all 90 browser/Axe/overflow tests across 1440/1024/768/390/320 pass.

This closes the approved mandatory-role MFA and active-membership self-disable gap. It does not authorize the unapproved facility-scope candidate or invent the optional viewer/editor organization-enforcement policy. M1B remains open for facility-scope approval/implementation, final visual/owner acceptance, target-environment evidence, and full slice acceptance.

## Phase 1AH progress - approved M1-20 responsive record-card projection resolved

| Initial finding                                                                                                                                                 | Implemented resolution                                                                                                                                                                                                                                                     | Current state                                                                                           |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| M1-20 kept its six-column action table inside a horizontal scroller at 390/320px, while the approved responsive contract requires record cards at drawer widths | The membership result region now switches at the approved 760px shell breakpoint: 768px and wider retain the keyboard-focusable table, while 390/320px render semantic membership record cards with administrator, role, access, MFA, effective-period, and action fields. | PASS in an explicit compact-projection unit test and the five-viewport browser matrix                   |
| Separate responsive markup could drift from the server-authorized desktop actions                                                                               | Both table rows and cards call one `MembershipActions` component and receive the same runtime-validated server projection. No action is inferred from viewport size, and facility-scope controls remain absent.                                                            | PASS for identical invitation, MFA-reset, role-change, revocation, and owner-transfer action projection |
| Responsive coverage proved containment but did not assert the approved table/card alternative                                                                   | The existing M1-20 browser case now asserts cards and no table at 390/320, then a table and no card list at 768/1024/1440, while retaining keyboard focus, Axe, filtering, and document-overflow checks.                                                                   | PASS across all five exact viewport projects                                                            |

Local evidence: a fresh Maven 3.9.11/Java 25 containerized clean verification compiles 232 production and 19 test sources, validates/applies all 24 migrations, passes all 174 backend tests with zero failures, errors, or skips, enforces all 11 architecture rules, and packages the JAR. Frontend generated-client drift, the 21-source/48-import architecture boundary and four negative fixtures, strict typecheck, lint, formatting, all 53 unit tests, the production build, and all 90 browser/Axe/overflow tests pass. The 79-screen, 31-operation, CI-security, approved-input, draft, candidate, and facility-scope-candidate repository verifiers also pass.

This closes the approved responsive table/card adaptation gap for M1-20 only. It changes no API, database, permission, membership mutation, or facility-scope behavior. M1B remains open for approval and implementation of the exact facility-scope candidate, remaining identity-screen visual reconciliation, explicit owner acceptance, target-environment evidence, and full slice acceptance.

## Phase 1AI progress - approved identity/access frame and content conformance resolved

| Initial finding                                                                                                                             | Implemented resolution                                                                                                                                                                                                               | Current state                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------- |
| The shared identity and workspace frames lacked the approved keyboard bypass and deterministic route-heading focus                          | Both frames now expose a keyboard-visible `Skip to main content` link. Activation focuses the existing `<main>` without changing the fragment-router URL, and each identity/workspace transition focuses its rendered `h1`.          | PASS in unit coverage and all five exact viewport projects                  |
| Automatic route-heading focus could overwrite a more useful focused problem summary                                                         | Heading focus now yields whenever focus is already inside the newly rendered main region, preserving the existing accessible error-summary contract.                                                                                 | PASS in existing failure-state tests and the full identity browser matrix   |
| The frontend loaded an unapproved Manrope face and several M1-01 through M1-04 primary labels/registry descriptions drifted from the source | The font stack is now the approved `Inter`, `Segoe UI`, `Roboto`, system stack. Registry titles/purposes and the `Continue securely`, `Invite administrator`, and `Open workspace` primary actions now match the approved artifacts. | PASS in exact-content unit tests and production build                       |
| Identity/access browser coverage did not explicitly prove the shared skip/focus contract at each approved width                             | Existing identity and workspace cases now assert initial heading focus, keyboard-visible skip navigation, main focus, and an unchanged URL at 1440, 1024, 768, 390, and 320 pixels.                                                  | PASS across 54 frontend unit tests and all 90 Playwright/Axe/overflow cases |

Local evidence: generated-client drift, the 21-source/48-import frontend architecture boundary and four negative fixtures, formatting, strict typecheck, lint, all 54 unit tests, the production build, and all 90 browser/Axe/overflow tests pass. The 79-screen, 31-operation, CI-security, approved-input, draft, candidate, and facility-scope-candidate repository verifiers also pass. No backend, OpenAPI, or database source changed in this increment; the immediately preceding fresh Maven 3.9.11/Java 25 baseline remains 174 passing backend tests through Flyway V24.

This closes the shared frame, focus, font, metadata, and primary-action content portion of M1-01 through M1-04. It changes no API, database, authorization, or facility-scope behavior. M1B remains open for approval and implementation of the exact facility-scope candidate, remaining screen-specific identity states and any separately authorized invitation inspection/resend/delivery operations, explicit owner acceptance, target-environment evidence, and full slice acceptance.

## Phase 1AJ progress - approved identity validation and M1-03 action conformance resolved

| Initial finding                                                                                                         | Implemented resolution                                                                                                                                                                                                         | Current state                                                                        |
| ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ |
| Local password-reset and invitation-acceptance errors were announced but stayed passive and were not field-associated   | A reusable focused identity-form summary now marks the exact invalid input with `aria-invalid`, associates its message through `aria-describedby`, and applies the approved two-pixel danger treatment.                        | PASS in focused unit coverage and all five exact viewport projects                   |
| A field link using its ordinary fragment destination would collide with the application's hash router                   | Each error summary exposes a real keyboard-operable field link, prevents route mutation, and focuses the invalid control directly; tests prove both field focus and an unchanged scrubbed URL.                                 | PASS for password-reset and invitation-acceptance validation                         |
| Malformed-token and local invitation/MFA preparation failures used non-focusable callouts                               | Missing/malformed reset and invitation links plus secure-randomness preparation failures now use the same focused durable summary. Server RFC 9457 summaries retain their existing safe correlation/retry behavior.            | PASS without changing any request, token, retry, or authorization behavior           |
| M1-03's remaining enrollment action said `Set up an authenticator` instead of the approved `Set up authenticator` label | The primary action now uses the exact approved content in normal, mandatory-enrollment, unit, and browser paths.                                                                                                               | PASS in both MFA enrollment paths and the full browser regression                    |
| The frontend architecture and identity regression evidence needed to include the new shared feature component           | The architecture verifier now accepts 22 source files and 49 relative imports; one new unit case and expanded identity E2E assertions cover focus, association, hash preservation, Axe, and overflow at 1440/1024/768/390/320. | PASS across 55 unit tests, 90 browser cases, and four negative architecture fixtures |

Local evidence: generated-client drift, formatting, lint, strict typecheck, production build, the 22-source/49-import frontend architecture boundary and four negative fixtures, all 55 unit tests, and all 90 browser/Axe/overflow cases pass. The 79-screen, 31-operation, CI-security, approved-input, review-draft, retained-candidate, and facility-scope-candidate repository verifiers plus their 56 combined negative tests pass. No backend, OpenAPI, database, permission, or facility-scope source changed; the current clean Maven 3.9.11/Java 25 baseline remains 174 passing tests through Flyway V24.

This closes the approved local identity-validation focus/association and remaining M1-03 primary-action content gaps. M1B remains open for approval and implementation of the exact facility-scope candidate, invitation inspection/resend/delivery behavior requiring separately authorized contracts, explicit owner acceptance, target-environment evidence, and full slice acceptance.

## Phase 1AK progress - exact approved readiness catalogue projection resolved

| Initial finding                                                                                                                 | Implemented resolution                                                                                                                                                                                                                                                                                                | Current state                                                                                |
| ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| M1-05/M1-06 still returned nine provisional keys and unapproved `in_progress`/`not_started` states                              | The server now emits all 15 approved keys in canonical `m1-readiness-v1` order and only `complete`, `warning`, `blocked`, or `not_applicable`. Domain invariants reject version, order, count, outcome, and 15-minute freshness drift.                                                                                | PASS in domain, HTTP, checked-contract, generated-client, and runtime-validator tests        |
| A provisional gate could appear complete even though its approved evaluator or authoritative entity family did not exist        | Only the already enforced final-owner and mandatory-role-MFA evaluators can complete. Missing organization, network, service, configuration, registry, and provider evaluators remain explicit blockers; undeclared delivery is a warning and undeclared identifier issuance is not applicable.                       | Fail-closed and truthful; this does not claim the missing evaluator implementations          |
| Readiness lacked approved version/freshness, stable reason/remediation codes, bounded evidence references, and safe drill-downs | The response carries catalogue version, organization revision, database evaluation/expiry instants, four outcome counts, per-gate version/codes, at most four aggregate evidence references, and a server-selected deep link. Links are filtered through the caller's live permissions and otherwise return to M1-06. | PASS including a readiness-only viewer that receives no unauthorized M1-20/M1-21 destination |
| M1-05 and M1-06 rendered one identical generic checklist                                                                        | M1-05 now provides the approved dashboard form with readiness/facility/lifecycle/freshness metrics and five priority exceptions; M1-06 renders the full ordered catalogue, freshness, and an explicit boundary separating this live projection from unimplemented persisted validation/activation.                    | PASS at 1440, 1024, 768, 390, and 320 with Axe and document-overflow checks                  |
| Contract/client drift could silently reintroduce approximate keys, outcomes, ordering, or arbitrary hash destinations           | OpenAPI 0.14.0 fixes the exact keys, outcomes, metadata, evidence bound, and 900-second freshness extension; the repository verifier has a new negative drift case, while the browser validator enforces canonical order and a per-gate destination allowlist before rendering.                                       | PASS across 8 API negative tests, 56 frontend unit tests, and all 95 browser cases           |

Local evidence: a clean Maven 3.9.11/Java 25 verification compiles 232 production and 20 test sources, validates and applies all 24 migrations, passes all 177 backend tests with zero failures, errors, or skips, enforces all 11 architecture rules, and packages the bootable JAR. OpenAPI 0.14.0 verifies all 31 operations and seven shared conventions, with all 8 negative tests passing. Generated API drift, the 22-source/49-import frontend boundary and four negative fixtures, formatting, strict typecheck, lint, all 56 unit tests, the production build, and all 95 five-viewport Playwright/Axe/overflow cases pass.

This is the first approved M1C increment, not completion of readiness validation or M1C. At that checkpoint the response remained a live projection, not an immutable `configuration_validation`: configuration versions/digests, persisted validation results, transaction-bound invalidation, full evaluator persistence for M1-07 through M1-19, evidence review, submission, maker-checker approval, activation, history, and export remained for M1C through M1F. Phase 1AL below resolves the exact M1-07 evaluator only.

## Phase 1AL progress - exact approved M1-07 organization profile resolved

| Initial finding                                                                                                 | Implemented resolution                                                                                                                                                                                                                                                                                    | Current state                                                                                         |
| --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| M1-07 omitted trading name, organization type, and locale and used provisional name bounds                     | Flyway V25 and the domain/API add the exact approved legal/display/trading identity, organization type, ISO country, IANA timezone, BCP 47 locale, lifecycle projection, revision, and updated time. Legacy missing type/locale remains visible but readiness-blocked until governed.                     | PASS through fresh PostgreSQL 18 migration, domain tests, checked OpenAPI, and response validation    |
| Approximate validation could accept unknown country/type/timezone/locale, unsafe names, or an invalid reason    | The service applies NFC normalization, exact code-point bounds, markup/control rejection for names, 10-500-code-point/control-free reason validation, approved type values, ISO country membership, IANA region identifiers, BCP 47 parsing/canonicalization, and stable `m1.field.*` Problem violations with exact field pointers. | PASS for exact invalid-field HTTP behavior and direct database constraint/trigger attacks             |
| Profile authority was not projected into the response and readers could be shown an unusable mutation surface | The store derives `editable` from live `organization.profile.manage`. M1-07 renders the exact fields for authorized owners/administrators/editors and an explicit usable read-only state for readers, with no role-name inference.                                                                       | PASS in frontend unit coverage and the complete five-width Axe/overflow matrix                        |
| The live profile readiness gate was permanently blocked even after a valid profile update                      | `organization.profile.complete` now evaluates exact required fields, optional trading-name validity, ISO/IANA/BCP 47 values, eligible lifecycle, and current revision. Legacy/ineligible profiles block; a valid governed update completes the gate and updates aggregate counts.                         | PASS before/after update in the real authorization/readiness integration flow                         |
| Audit/replay evidence needed to cover the expanded profile without leaking values                              | Strong ETag, caller idempotency, exact replay, stale conflict, explicit reason, and one atomic audit/outbox pair remain enforced. The payload contains only alphabetically sorted `changedFields` plus `lockVersion`, and exact replay does not duplicate evidence.                                      | PASS for update, replay, changed-request conflict, stale revision, sorted payload, and evidence count |
| Contract drift could silently weaken field bounds, type/lifecycle vocabularies, nullability, or editability    | OpenAPI 0.15.0 fixes exact response/update shapes and adds an eighth negative drift fixture, bringing the suite to nine tests with its acceptance case. Generated types, the native client, runtime UI validation, server validation, domain construction, migration constraints, and integration assertions now cover the same approved boundary. | PASS across all repository, API, backend, frontend, browser, and security gates                       |

Local evidence: a clean Maven 3.9.11/Java 25 verification compiles 232 production and 21 test sources, validates and applies all 25 migrations, passes all 181 backend tests with zero failures, errors, or skips, enforces all 11 architecture rules, and packages the bootable JAR. OpenAPI 0.15.0 verifies all 31 operations and seven shared conventions; all 9 contract tests pass, comprising one acceptance case and eight negative drift fixtures. Generated API drift, the 22-source/49-import frontend boundary and four negative fixtures, formatting, strict typecheck, lint, all 57 unit tests, the production build, and all 95 five-viewport Playwright/Axe/overflow cases pass. The 79-screen, approved-input, review/candidate, facility-candidate, and CI-security gates pass.

M1-07 is no longer an implementation gap. M1C remains open for M1-08 registration identifiers, M1-09 effective addresses/contacts, M1-10 international-settings change behavior, M1-11 governance responsibilities, their readiness evaluators, configuration-version invalidation, and final owner/target acceptance. Immutable validation/submission/approval/activation/history/export remains M1F work.

## Data model and database gaps

The current migrations are deliberately labelled a foundation. They must be extended by controlled, additive migrations as modules are implemented.

- Retain the implemented UUIDv7 strategy, and add the remaining standard columns and constraints required by the specification: actor columns, non-null tenant IDs for tenant-owned records, revisions, effective-date ranges, status constraints, reasons, supersession lineage, composite foreign keys beyond the implemented scan-attestation/object-evidence link, and appropriate indexes.
- Retain V14 final-owner and V17 MFA maker-checker enforcement plus the append-only general audit/outbox controls; extend equivalent safeguards to business lifecycle approvals, prohibit effective-date overlaps where required, and prevent destructive cascades into historical records.
- Retain the V19 environment-scoped seed boundary: synthetic organization/facility rows are allowed only in local/test, while default/production paths remove untouched legacy fixtures and fail closed for changed or referenced data. Add an approved tenant-provisioning and initial-owner ceremony before production onboarding.
- Extend the PostgreSQL 18 migration/RLS suite with every new table, constraint, role, and policy; the current suite covers the 39-table foundation through `V25`, including all 17 UUIDv7 defaults, checksum-bound approved and retained reference catalogs, final-owner/delegation policy, governed invitations, non-owner membership changes and owner transfers, separate recent-MFA enforcement, mandatory-role MFA projection and factor-disable protection, service identities, maker-checker approval/consumption, exact organization-profile/membership/owner-transition constraints and triggers, approved membership read, scoped foundation fixtures, governance/notification/document evidence, concurrency, and cross-tenant attacks.
- Add lifecycle schemas and state transition controls before exposing each workflow. Tables alone do not satisfy the state-machine, reason, approval, audit, and outbox rules.

## Frontend and prototype-to-product gaps

The frontend now has drift-checked generated contract types, a secure native-fetch client for all 31 implemented operations, and CI-enforced `session` and `administration` feature boundaries. Login, pending MFA, locked mandatory-role enrollment, organization selection, password recovery, authenticated MFA self-service, invitation issue/revoke/acceptance/linkage, maker-checker administrator reset, server-deadline lock, event-driven resume revalidation, exact-catalogue organization readiness, exact approved profile read/update, M1-20 membership read, governed organization-wide non-owner role-change/revocation, and final-owner-safe owner-promotion/demotion request/approval/execution are server-driven. M1-05 renders server metrics and priority exceptions; M1-06 renders the complete ordered live catalogue and rejects key/order/count/freshness/link drift; M1-07 renders the approved fields in editable or permission-projected read-only mode and rejects malformed profile/ETag responses. M1-05 through M1-07 and M1-20 are real bounded flows; the other 71 protected entries still use generic `Dashboard`, `DataList`, `FormScreen`, and `ClinicalScreen` templates. Those pages are explicitly marked synthetic, warn against real-data entry, use unambiguous sample identities, keep generic form/clinical fields read-only, disable every unimplemented workflow action, and retain only local filtering and valid prototype pagination. There is no general tenant business-record cache, persisted readiness-validation workflow, or complete production business module yet.

Required work:

- Replace the remaining hash-only prototype router with an approved route structure and add screen-specific protected business loading/empty/denied/conflict/error/success behavior. Authenticated unregistered hashes now fail closed to a focused, non-reflective not-found page after the session and organization gates; this boundary is not a substitute for approved product routing.
- Extend the session-level shell, organization switcher, form controls, error summary, loading/empty states, and implemented recent-authentication pattern into the specified approved Design System components: data table/card fallback, cursor pager, confirmation/reason dialogs, approval panel, evidence viewer, timeline, toast, and skeleton states.
- Connect approved business screens to the checked client and add tenant-safe server-state caching plus organization partitioning/invalidation. Session/organization, expiry, and identity self-service transitions are memory-only and implemented; do not retain hard-coded production-style records, bypass the shared session-lifecycle publisher, or add client-only workflow actions.
- Implement screen-specific forms, server validation/error focus management, autosave/conflict behavior where specified, filtering/sorting/cursor pagination, and permission-driven action visibility. M1-20 now supplies the server-backed filter/cursor/action-projection pattern; extend it only through approved per-screen contracts.
- Replace generic dashboard/list/form content screen by screen. The repository now contains the approved Module 1 Design System and M1 mockups, but most M1 screens have not yet been implemented from them; approved Module 2 designs and the original protected COS assets remain unavailable. The prototype register identifies current M2/COS content as synthetic placeholders requiring reconciliation.
- Replace the deliberately disabled dashboard `Review`, list `Open`, form save, and clinical save/confirmation controls only when their approved server workflows, authorization, persistence, evidence, error/conflict states, and acceptance tests exist. M1-20 filtering is now server-backed; retain local filtering only on explicitly synthetic prototype lists. Sign-out and organization switching already use server APIs.
- Retain the implemented 1440/1024/768/390/320 reference matrix and document-overflow guard while extending approved screens with their specified responsive table/card alternatives, complete keyboard workflows, dialog focus transitions, and formal WCAG 2.2 AA acceptance.

## Module delivery ledger

No business module is complete. Implement only in dependency order after Phase 0 acceptance and approved module inputs.

### Module 1 preimplementation checkpoint

The required architecture, mockup/source review, screen-specific gap ledger, executable eight-artifact approval contract, checked owner-review drafts, exact approved `m1-candidate-1` package, and implementation slices are recorded in `docs/MODULE_1_IMPLEMENTATION_PLAN.md`. The production manifest reports `APPROVED`, 8/8, and implementation authority. V20 promotes the approved interactive policy plus existing organization-core and identity-administration bindings; V21 implements the approved M1-20 membership read; V22 implements governed organization-wide non-owner role changes and revocation; V23 implements final-owner-safe owner promotion/demotion; V24 implements approved mandatory-role MFA enrollment/use and self-disable protection; V25 implements the exact M1-07 organization profile. M1-08 through M1-19 and M1-21 through M1-23 remain generic templates, and catalogue entries do not count as delivered behavior. M1B is in progress; M1C-M1G have not met their exit conditions.

| Phase/module                                    | Current state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Work remaining for completion                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Phase 0 - foundation                            | Repository mechanics complete; approved M1 interactive authorization release added; target-environment production acceptance pending                                                                                                                                                                                                                                                                                                                                                                                                              | Provision/rotate service credentials; accept production document/Redis/key-management/TLS/secrets/edge controls; add real workers/providers/transports, monitoring/alerting/backup/restore/deployment evidence, hosted-CI/registry enforcement, and bind future business transitions to approved composite tenant/governance rules.                                                                                                                            |
| M1 - Administration (`M1-01` to `M1-23`)        | Input gate approved 8/8; V20 release plus invitation/MFA increment complete; V21 M1-20 membership list/read complete; V22 organization-wide non-owner change/revocation complete; V23 final-owner-safe owner transfer complete; V24 mandatory-role MFA enforcement complete; V25 exact M1-07 fields/persistence/readiness complete; exact M1-05/M1-06 live readiness is implemented; facility-scope candidate checked but not approved; M1B and the rest of M1C remain in progress | Approve exact `m1-facility-scope-candidate-1`, implement its facility-scoped grant/scope change plus accepted UI, then M1-08 through M1-11 identifiers/contacts/settings/governance, facilities/departments/locations/hours/holidays, services, identifier schemes, persisted validation/activation, maker-checker, configuration history, audit filters, and purpose-bound export against the approved schema, transitions, permissions, evidence, tests, and UI states. |
| M2 - Workforce (`M2-01` to `M2-29`)             | 29 prototype routes only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Implement the specified 44-table model, organization-scoped duplicate matching, clinical/non-clinical onboarding, engagements, practitioner data, credentials/evidence/independent review, scopes/maker-checker, assignments/transfers, service eligibility, access, availability, lifecycle/offboarding, expiry automation, history/audit/export, and controlled registries.                                                                                  |
| M3 - Patient Registry                           | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Build patient identity, duplicate governance, contacts, preferences, identifiers, caregiver/proxy authorization, consent/privacy, safety flags, registration/merge processes, timeline, APIs, and 16 screens.                                                                                                                                                                                                                                                  |
| M4 - Appointments                               | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Build schedules/slots, atomic holds and booking, assignment eligibility at appointment time, reschedule/cancel/no-show policy evidence, waitlist, reminders, external calendar links, APIs, and 15 screens.                                                                                                                                                                                                                                                    |
| M5 - Encounters                                 | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Build episodes/encounters, participant snapshots, notes/versioning/signatures/amendments, tasks/orders, clinical state machine, signer eligibility, red-flag escalation, APIs, and 12 screens.                                                                                                                                                                                                                                                                 |
| M6 - Clinical Assessment (`COS-01` to `COS-27`) | 27 synthetic placeholder routes only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Obtain/protect original COS assets; import exact workflow/copy/components/navigation; implement assessment sessions/responses/versioning/instruments/measurements/red flags/reviews/signatures/amendments, debounced conflict-aware autosave, clinician sign-off, and clinical safety controls.                                                                                                                                                                |
| M7 - Documents and Results                      | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Implement private upload intent, quarantine/validation/scan/promotion, versioning, controlled viewer/download, result provenance, critical-result acknowledgement/escalation, retention/legal hold, APIs, and 11 screens.                                                                                                                                                                                                                                      |
| M8 - AI Assistance                              | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Implement purpose/consent/input manifest controls, separate Python-service job contract, data minimization, model/prompt/evaluation governance, draft-only outputs, provenance/uncertainty/safety flags, clinician review/approval, audit/retention/cost records, APIs, and 10 screens.                                                                                                                                                                        |
| M9 - Care Planning                              | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Implement versioned plans, goals, interventions, owners/tasks, preferences/consent, safety/interaction review, approval/amendments, APIs, and 12 screens.                                                                                                                                                                                                                                                                                                      |
| M10 - Follow-up and Outcomes                    | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Implement outcome definitions/baselines/measures, follow-up schedule, thresholds, owned escalation/acknowledgement, interpretation/timeline, APIs, and 9 screens.                                                                                                                                                                                                                                                                                              |
| M11 - Billing and Payments                      | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Implement price books/packages/estimates/invoices/payments/refunds/claims/reconciliation, provider webhooks with signature/idempotency, immutable finance audit/export, APIs, and 11 screens.                                                                                                                                                                                                                                                                  |
| M12 - Reporting and Governance                  | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Implement authorized minimum-necessary projections, report scheduling, purpose/recent-auth controls, safe exports/formula-injection defense, audit/history, observability views, APIs, and 10 screens.                                                                                                                                                                                                                                                         |
| M13 - Integrations                              | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Implement versioned FHIR mappings/profiles/provenance, messaging, payments, calendars, labs/imaging, webhook replay protection, mobile/API clients, outbox delivery/dead-letter/replay, APIs, and 10 screens.                                                                                                                                                                                                                                                  |

## Security, privacy, and clinical-safety gaps

- Implement purpose/legal-basis tracking, field-sensitivity policy, jurisdiction-specific retention/disposal/legal-hold controls, consent withdrawal behavior, patient access/correction workflow, and privacy review.
- Retain the implemented recent-authentication/reason and V17 maker-checker pattern, then apply it to every approved high-risk lifecycle, access, export, and finance action; decide separately whether organization policy permits self-service MFA disable.
- Phase 0O adds required production secret injection, rejects documented non-production material and insecure dependency/browser settings, and enforces backend/frontend security headers plus strict same-origin CSP. Still deploy and exercise an approved secret manager, key rotation/re-encryption, public and service-to-service TLS/trusted-proxy topology, private management boundary, per-operation/application and coarse edge rate limits, WAF/DDoS controls, CSP reporting, and security/load scans in the target environment.
- Complete production acceptance for the opt-in private quarantine, scanner, clean-promotion, signed-access, and immutable-retention adapters; replace synthetic promotion/access/retention settings with approved policy and bind the implemented evidence mechanics to a governed permission/event/document-state/read-audit workflow. Add separately governed legal-hold release and disposal only after jurisdiction-specific approval; never make private clinical/credential content public or send it to AI without a separately authorized purpose.
- Implement clinical red-flag/task/escalation workflows; do not permit unreviewed AI output, client-only sign-off, or synthetic clinical content in production workflows.
- Conduct required legal, privacy, clinical-governance, and regulatory reviews. The specification explicitly leaves policy decisions open; code cannot safely infer them.

## CI, runtime, and operations gaps

- Maven Wrapper verification, ArchUnit, Testcontainers Flyway/RLS/identity tests, checked OpenAPI, Playwright/Axe, immutable-workflow checks, dependency review, CodeQL, Trivy source/image scanning, CI image builds, and CycloneDX SBOM generation are now configured CI gates. Remaining work is a successful target-repository run, required-check/rule enforcement, per-operation approved permission/event checks as slices are implemented, and relevant performance/security testing.
- Every maintained Dockerfile base, Compose/overlay service, and PostgreSQL/Redis Testcontainers dependency is pinned by digest. Dependabot covers each image location; production still needs an approved refresh cadence, signed provenance, registry admission, deployment verification, and emergency patch procedure.
- The repository-local signal baseline now has structured ECS JSON, safe request/trace correlation, authenticated Prometheus format, status-only liveness/readiness probes, an outage drill, and `docs/OPERATIONS.md`. V16 supplies a reference service-authorization boundary; still provision and deploy authorized worker/scheduler processes, distributed locks, job/notification dead-letter review/replay, consent-aware outbound delivery, an approved scraper identity/private management boundary, external metric/trace/log collection, measured service objectives, alert routing/on-call tests, encrypted backups and timed restores, and environment/provider-specific runbooks.
- Add deployment assets and procedures for managed PostgreSQL/Redis/private storage, secret manager, TLS, signed images, SBOM/provenance verification, backwards-compatible migration jobs, rollback/forward-repair, and tenant-isolation smoke checks.
- Node tooling is now declared at `24.15.0` for local version managers, Docker, and CI. The audit host remains on `24.13.0` and therefore still emits the expected `EBADENGINE` warning until its local runtime is upgraded.

## Required owner inputs and decisions

These are genuine blockers, not implementation details to guess:

1. Original COS-01 through COS-27 source assets and checksum/protection process.
2. Slice-level product acceptance of each implemented M1 screen/workflow and a separate approved Module 2 input package; the exact Module 1 input disposition itself is complete.
3. Target-environment authorization/identity operating approval, including production account provisioning, support ownership, MFA recovery operations, service credentials, and emergency procedures; the Module 1 policy catalogue itself is approved.
4. Jurisdiction-specific privacy, retention, residency, legal-hold, identity-proofing, and cross-organization matching policies.
5. Credential/supervision matrices, break-glass design, critical-result/red-flag SLAs, notification consent/escalation policy, and cancellation/no-show/refund policy.
6. AI provider/region/retention/training/evaluation policy and approved safety thresholds.
7. Production hosting, RPO/RTO, monitoring budget, ownership/staffing, and secrets/key-management decisions.

## Recommended implementation sequence

1. **Accept Phase 0 for the target environment:** repository mechanics and the approved interactive policy are complete locally; capture target-environment CI, security, provider, monitoring, backup/restore, identity operations, and deployment evidence before recording a production PASS.
2. **Finish M1B, then deliver M1C-M1G in order:** retain required-approval verification plus the completed M1-20 read, organization-wide non-owner role-change/revocation, final-owner-safe transfer, and mandatory-role MFA boundaries; approve exact `m1-facility-scope-candidate-1`, implement it, and obtain final identity/access UI acceptance; then deliver tenant-safe organization foundations, network/services/configuration, and activation/history/audit/export against the exact approved contracts.
3. **Freeze approved M2 inputs, then deliver M2:** reuse M1 identity/RBAC/configuration instead of creating parallel models.
4. **Proceed M3 through M13 strictly in specification dependency order.** Each module needs a gap ledger, traceability matrix, approved policy/mockups, real persistence, tests, and owner acceptance before the next module begins.
5. **Treat clinical/COS and AI work as safety-sensitive:** do not use the placeholder UI as a visual or workflow freeze.

## Definition of complete for any item above

Mark an item complete only when its approved route/screen, permission, handler/service, database migration, state transition, validation, audit/outbox record, security controls, responsive/accessibility states, and automated tests all exist and pass. Client-only behavior, an unreachable endpoint, a schema without an enforced workflow, or a skipped critical test is **not** completion.

## Verification snapshot

Run during this audit:

| Check                                                  | Result                 | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npm ci` in the pinned Node 24.15 image                | PASS                   | Installed 289 Linux-platform packages and audited 290; npm reported 0 vulnerabilities without an engine mismatch. The host-runtime warning noted above remains a local tooling issue.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `npm run api:check`                                    | PASS                   | Both generated TypeScript artifacts exactly match the checked OpenAPI 3.1 source.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `npm run architecture:check`                           | PASS                   | The verifier accepts all 22 frontend source files, both bounded features, and 49 relative imports in the feature/page/component/data/API direction; all four mutation fixtures are rejected as expected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `npm run typecheck`                                    | PASS                   | Strict TypeScript build completed with `noUncheckedIndexedAccess`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `npm run lint`                                         | PASS                   | Zero warnings permitted and produced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `npm run format:check`                                 | PASS                   | Prettier reports all matched files formatted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `npm test`                                             | PASS                   | 2 files / 57 tests cover the checked transport/session lifecycle, mandatory-role enrollment/recovery-code lock, identity transitions, frame/focus/navigation behavior, exact ordered readiness validation, exact profile transport/runtime validation/update/read-only projection, M1-20 query and mutation validation, compact record cards, ETag/idempotency/stale recovery, expiry/revalidation, denial/retry, logout, strict registry lookup, unknown routes, honest synthetic actions, and non-activatable terminal pagination. |
| `npm run build`                                        | PASS                   | Vite production build completed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `npm run test:e2e`                                     | PASS                   | 95 tests pass across exact 1440, 1024, 768, 390, and 320 pixel projects. Identity cases prove focused password-reset/invitation validation, exact invalid-field association, hash-safe field navigation, route-heading focus, keyboard-visible skip navigation, Axe, and containment at every width. All 79 registered routes are Axe/overflow checked; five readiness cases prove distinct M1-05 dashboard and M1-06 exact-catalogue projections; five mandatory-role enrollment cases keep the workspace locked through recovery-code storage; fifteen M1-20 cases verify real loading/data, server filtering, cursor paging, permission-projected navigation, approved table/card alternatives, exact governed request transport, Axe, and containment. The suite retains candidate-mockup, recovery, invitation, MFA, session, profile, and synthetic-boundary guarantees. |
| Default and scanner-overlay `docker compose config -q` | PASS                   | Both Compose models resolve, including the pinned private-network ClamAV overlay and the supported PostgreSQL 18 parent-volume target. The security contract rejects the legacy target.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `docker compose build --quiet backend frontend`        | PASS                   | Both current application images build successfully from the checked Docker contexts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Isolated hardened backend deployment smoke             | PASS                   | The current distroless image reached `UP` liveness/readiness against fresh PostgreSQL/Redis/Mailpit/MinIO at Flyway V18, seeded one synthetic organization, ran as `65532:65532`, rejected a root write and anonymous metrics, accepted its bounded `/tmp` write, dropped all capabilities, and retained no-new-privileges.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Isolated hardened frontend deployment smoke            | PASS                   | The current Nginx image reached healthy as `nginx`, served the strict header policy, proxied all 79 registered screens, rejected a root write, accepted its bounded `/tmp` write, dropped all capabilities, and retained no-new-privileges.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Maven 3.9.11 `clean verify` on Java 25 with Docker     | PASS                   | The clean verification compiled 232 production sources and 21 test sources, passed all 181 tests with zero failures, errors, or skips against disposable PostgreSQL 18, Redis 8, Object-Lock-capable storage, and deterministic ClamD fixtures, applied Flyway through V25, enforced all 11 architecture rules, and packaged the bootable JAR. Coverage includes exact readiness and organization-profile domain/HTTP/database/evidence behavior, mandatory-role MFA, promotion invalidation, factor-disable rejection, governed reset, membership cursors/actions/approval/transitions, final-owner/direct-SQL rejection, invitation/MFA lifecycles, and cross-tenant attacks. |
| API contract verifier and negative tests               | PASS                   | OpenAPI 3.1 version 0.15.0, all 31 operations, local references, cookie/CSRF/correlation/Problem/concurrency/idempotency requirements, exact readiness and organization-profile contracts, and seven shared conventions validate; all 9 contract tests pass (one acceptance case and eight negative drift fixtures). |
| Module 1 input-package contract                        | PASS as `APPROVED`     | The schema and manifest bind all eight exact artifacts, all M1-01 through M1-23 screens, approval record `M1-APPROVAL-20260916-01`, and distinct approval evidence at package digest `19aff5ce30516b7ee2101c093a8429d8a74394995ca90d486790bcc18a392946`. Both verifier modes and all 14 tests pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Module 1 owner-review draft packet                     | PASS as draft only     | All eight canonical review briefs validate at digest `bee95ca0ddb74d14256d2dd80fe9565021e64a9f6feb6124b7a1110335a3fe63`; all 7 tests pass and the verifier reports `implementationAuthorized: false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Module 1 candidate input package                       | PASS as candidate only | All eight candidate artifacts validate at digest `c2087548aacd35eb4927532d63b844851c6fe9c56cccb56e46596d707d7a1a53`; all 8 tests and five viewport browser cases pass while the verifier reports `implementationAuthorized: false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Module 1 facility-scope additive candidate             | PASS as candidate only | The exact contract validates at digest `76a3f7a2c63cef0cab02b8a1d38a66220eee0abb99c9be0a82b64eaa8941fddb`; all 7 tests pass and the verifier reports `CANDIDATE_FOR_APPROVAL` plus `implementationAuthorized: false`. No runtime behavior is claimed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Prototype and CI-security repository contracts         | PASS                   | The 79-screen register validates; the security verifier accepts both workflows, generated API/frontend architecture, the production Module 1 input gate, separate draft, retained candidate, and additive facility-scope gates, exact responsive matrix/overflow assertions, maintained image pins/update entries, Nginx security policy, and environment-scoped foundation data; all 13 security-contract tests pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Trivy 0.74 filesystem scan                             | PASS                   | The current Maven and npm manifests report 0 fixed HIGH/CRITICAL findings, both Dockerfiles report 0 misconfigurations, and no secret finding is emitted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Trivy 0.74 final-image scans and CycloneDX generation  | PASS                   | The current Debian 13.6 backend OS/JAR and Alpine 3.24.1 frontend report 0 fixed HIGH/CRITICAL findings. Parsed CycloneDX 1.7 outputs contain 199 backend and 22 frontend components; fresh hosted scans and retained exact-artifact SBOMs remain required for production acceptance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

---

**Bottom line:** the repository-level Phase 0 mechanics are complete and locally green, the exact Module 1 package is approved, V20 records its immutable authorization release, V21 implements approved M1-20 membership list/read, V22 implements governed organization-wide non-owner role changes/revocation, V23 implements final-owner-safe owner promotion/demotion, V24 enforces approved mandatory-role MFA plus governed factor removal, and V25 implements the exact approved M1-07 organization profile. M1-20 follows the approved responsive table/card alternative, and M1-01 through M1-04 follow the approved shared frame/content/focus/validation contracts. Phase 1AK starts M1C with the exact fail-closed 15-gate live-readiness projection; Phase 1AL adds exact profile persistence, validation, permission editability, evidence, UI, and the real profile-complete evaluator without claiming persisted configuration validation or activation. OpenAPI 0.15.0, the generated client, 181 backend tests, 57 frontend unit tests, and all 95 browser/Axe cases pass. The missing facility-scope semantics are captured by an exact CI-checked additive candidate, but it has no approval or implementation authority. Optional viewer/editor organization-level MFA policy is likewise not invented. The repository retains digest-bound invitation/MFA administration, environment-scoped synthetic data, fail-closed routing, honest action-safe generic pages, browser identity/session/recovery/MFA, organization selection, final-owner/delegation controls, tenant RLS, audit/outbox/idempotency, fail-closed platform adapters, checked API/client boundaries, and repository security gates. This is **not production acceptance**: M1B still needs approval and implementation of `m1-facility-scope-candidate-1`, separately authorized invitation inspection/resend/delivery behavior, and explicit owner/target-environment acceptance; M1C still needs M1-08 through M1-11 and final acceptance; M1D-M1G, production providers/infrastructure, and hosted operational evidence remain incomplete. No M1-M13 or COS product module is complete.
